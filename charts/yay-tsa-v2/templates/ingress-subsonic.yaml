{{- if and .Values.ingress.enabled .Values.backend.enabled .Values.ingress.subsonic.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "yay-tsa-v2.fullname" . }}-subsonic
  labels:
    {{- include "yay-tsa-v2.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.ingress.className }}
    kubernetes.io/ingress.class: {{ .Values.ingress.className }}
    {{- end }}
    traefik.ingress.kubernetes.io/router.priority: "20"
    {{- if .Values.ingress.secureHeaders.enabled }}
    {{- $namespace := .Release.Namespace }}
    traefik.ingress.kubernetes.io/router.middlewares: {{ printf "%s-%s-secure-headers@kubernetescrd" $namespace (include "yay-tsa-v2.fullname" .) }}
    {{- end }}
    {{- with .Values.ingress.subsonic.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range $.Values.ingress.subsonic.paths }}
          - path: {{ . }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "yay-tsa-v2.fullname" $ }}-backend
                port:
                  number: {{ $.Values.backend.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
