# LEVERAGE — /review-leverage findings log

## 2026-06-14 14:30 · 90dd7b34 · MODERNIZE TOOLING + STRUCTURE (toolchain inventory, infra-metadata-enricher, BuildSha plumbing)

### TL;DR

Toolchain is largely modern and well-chosen (ESLint 9 flat config + Prettier, Vitest 4, ruff, ktlint-via-Spotless). The headline leverage is **dead/redundant tooling, not legacy-tool swaps**: detekt is installed + configured but surgically removed from the `check` lifecycle (never runs), and the JS/TS pre-commit stack carries 2 hand-rolled bash hooks + a duplicate Python security hook that real lint rules already subsume. The new `infra-metadata-enricher` module is structurally convention-compliant; its only real issue is HTTP-client/retry duplication shared with 3 other modules. The BuildSha plumbing is justified by the PWA rollout-gate contract — leave it; only a dead build-arg is a clean fix. Recommended swaps to Biome/oxlint/pnpm are REJECTED on parity grounds.

### Top Issues

1. ✅ **detekt is dead — applied + configured but never runs.** `yay-tsa-v2/build.gradle.kts:33` applies the plugin to every leaf subproject, `:38-41` configures it from `detekt.yml` (real 563-byte config exists), but `:62-65` surgically prunes it from the `check` lifecycle: `tasks.named("check") { setDependsOn(dependsOn.filterNot { it.toString().contains("detekt") }) }` with comment "Detekt 1.x is incompatible with Kotlin 2.1". `gradle/libs.versions.toml:13` pins `detekt = "1.23.7"`. Web-verified (June 2026): **detekt 1.23.8 is compatible with Kotlin 2.1/2.2**, and the codebase is Kotlin 2.1.20 — so the disabling rationale is now false. Result: dead plugin + orphan config, zero enforcement (CI runs `./gradlew build` which never invokes it). 🟡 (silent loss of a quality gate, not a security/reliability risk — re-rated down from scout's 🔴). **Fix:** bump to 1.23.8 in `libs.versions.toml:13`, delete the prune block (`build.gradle.kts:62-65`), generate `detekt --create-baseline` so the gate goes green immediately, then triage incrementally. Verify `detekt.yml` parses under 1.23.8 (schema shifts across minors). Do NOT jump to detekt 2.0 (alpha, Kotlin 2.4). Effort: 10min config + 1-3hr triage. Highest-leverage finding.

2. ✅ **Hand-rolled `no-console` bash hook redundant with ESLint.** `.pre-commit-config.yaml:239-246` greps for `console.log|debug` and exits 1. ESLint already has the rule but at `'warn'` (`eslint.config.js:42,124,272` — `'no-console': ['warn', { allow: [...] }]`). Setting it to `'error'` is a strict superset of the bash hook (also covers core/platform, not just `apps/web/src/{features,routes,components}`). 🟡 **Fix:** flip ESLint `no-console` to `'error'` (keep `allow` list to preserve `info`), delete the bash hook. Easy, always-yes. (Confirm no intentional `console.log` exists in core/platform first.)

3. ✅ **Standalone `bandit` hook duplicates ruff.** `.pre-commit-config.yaml:144-151` runs bandit; `services/audio-ml/pyproject.toml:18` already selects ruff `"S"` (flake8-bandit) with the same skips duplicated in both ruff-ignores and a `[tool.bandit]` block. 🟡 **Fix:** drop the bandit hook + `[tool.bandit]` block, after confirming ruff `S` covers bandit `B615` (HF `from_pretrained`). Easy.

### Other findings

- 🟡 **HTTP-client + retry duplication (infra-metadata-enricher F1/F2/F3 merged).** Four outbound JDK clients duplicate `HttpClient.newBuilder` + User-Agent + timeout + `runCatching{send}` + status-`when` boilerplate: `MusicBrainzClient.kt:71-76`, `CoverArtArchiveClient.kt:22-28`, `infra-karaoke-worker/SeparatorClient.kt:24-29`, `adapter-jellyfin/LrclibClient.kt:21-25`. No `infra-http` module exists. `MusicBrainzClient.backoff()` (`:176-185`) is the only Kotlin backoff-with-jitter (a new wheel). **Latent reliability gap:** `CoverArtArchiveClient.fetch()` (`:48-53`) throws on first failure with NO retry loop, while MusicBrainz retries-then-throws — inconsistent against a flaky external service. **Fix:** (a) always-yes first step — unify CoverArt to retry via the same backoff; (b) extract a parameterized `infra-http` helper (keep deliberate per-caller variations: HTTP_1_1, redirect policy) when touching the 2nd caller. Effort: F3 small / F1 medium.
- 🟡 **Dead `GIT_SHA` build-arg for the backend image.** `.github/actions/docker-build/action.yml:121-123` passes `GIT_SHA` as a Docker build-arg, but `yay-tsa-v2/Dockerfile` has zero `ARG GIT_SHA` (verified grep -c = 0). The sha actually reaches the pod via a Helm env var (`charts/yay-tsa-v2/templates/backend-deployment.yaml:63-64`, `value = image.tag`), not at build time. Misleading no-op. **Fix:** drop the build-arg for the backend path (or add the ARG if future build-time use intended). Easy.
- 🟡 **`lodash` override `"4.18.1"` (`package.json:45`)** — off-mainline phantom version above the maintained `4.17.x` security line. Exact pin is suspicious/likely a typo. **Flag for human review** (run `npm ls lodash`); don't auto-edit.
- 🟡 **`jsdom ^29` stale major** (`package.json:68`, `apps/web/package.json:46`) while the rest of the stack is current. Easy bump (run web tests).
- 🟡 **`component-naming` grep hook** (`.pre-commit-config.yaml:248-256`) subsumable by `eslint-plugin-check-file` filename rule. Easy but adds a dep — case-by-case.
- 🔵 **knip + jscpd + type-coverage run full-repo scans per commit** (`.pre-commit-config.yaml:215-237`, all `pass_filenames: false`) plus 2 `tsc` runs. Move to `stages: [pre-push]` like `npm-audit` already is (`:206`) — faster every commit, same coverage. Easy. **Highest-ROI single move.**
- 🔵 **`apps/web` lint `--ext` dead flag** (`apps/web/package.json:10`) — ignored under ESLint 9 flat config. Trivial cleanup.
- 🔵 Kotlin 2.1.20 / Spring Boot 3.4.4 stale (Kotlin 2.4 / Spring Boot 4 GA June 2026); Spotless 7.0.2 minor behind (8.6.0 out); `build-logic` duplicates 4 version pins from `libs.versions.toml` (own TODO acknowledges). Notes — separate dependency-bump effort.

### Systemic Patterns

1. **Disable-don't-delete tooling debt** — detekt and the dead build-arg are both "wired but inert". The fix is recover-or-remove, not leave-half-on.
2. **Hand-rolled bash hooks substituting for lint rules** — `no-console` and `component-naming` reimplement in brittle `find`/`grep` what a declarative rule expresses. Real rules subsume them.
3. **Per-module HTTP boilerplate** — outbound HTTP grew organically into 4 near-identical hand-built JDK clients across 3 modules with divergent retry semantics.

### False Positives / Rejected

- **Biome / oxlint swap — REJECTED.** `eslint-plugin-boundaries` (`eslint.config.js:7,279-343`) enforces the Core→Platform→Web layering; Biome has no equivalent (hard parity blocker), its Tailwind-sort is nursery/unsafe, and oxlint type-aware is alpha (not GA, June 2026). Keep ESLint+Prettier — the "two tools" is essential, not accidental, complexity here.
- **npm → pnpm** — marginal at 4 workspace packages; case-by-case, not forced.
- **tsgo** — TS 7 native port is Beta (not GA, June 2026); optional CI-typecheck-only experiment, can't replace `tsc -b` emit.
- **semgrep removal — REJECTED (scout overstated overlap).** ESLint config imports NO security plugin; semgrep `p/javascript`/`p/typescript` carry taint/injection rules ESLint lacks. At most drop the `p/react` line; keep the rest.
- **BuildSha value-source rework — REJECTED.** The custom `ServerInfo.BuildSha` field (`JellyfinDtos.kt:88`, `JellyfinAuthController.kt:146`) is justified: the PWA rollout gate (`v2-ci.yml`) polls `/api/System/Info/Public` for PascalCase `.BuildSha == main-<sha7>`. Actuator `/actuator/info` has a different path/shape/port and won't satisfy the contract. The deployed value = image.tag (what Image Updater rolled out, correct format); Spring `buildInfo()`/`GitProperties` would report raw source-HEAD hex, wrong format, **breaking the gate**. Leave the source plumbing as-is.
- **infra-metadata-enricher `core-application:shared` dep, package name, wiring, inline Jackson DTOs, manual `@Component` client construction** — all convention-compliant with sibling workers. Non-issues.

### Total Estimated Savings

- Pre-commit hooks removable: **3** (no-console, bandit, component-naming) + **3 relocated** to pre-push (knip, jscpd, type-coverage)
- Config blocks removable: **2** (`[tool.bandit]`, detekt prune block)
- Dead flags/args removable: **2** (lint `--ext`, GIT_SHA build-arg)
- Tools to recover: **1** (detekt — whole Kotlin static-analysis gate)
- Refactors (DRY/resilience): **2** (shared infra-http helper, cover-art retry)
- Tool swaps rejected on parity: **3** (Biome/oxlint, pnpm, tsgo)
- Commit-time impact: faster every commit (3 full-repo scans → pre-push, 2 bash hooks gone)

### Verdict

Modern stack with localized debt: re-arm detekt, delete the redundant bash/bandit hooks, unify the HTTP-client retry path, drop the dead build-arg — leave the (justified) ESLint+Prettier stack and BuildSha contract alone.

Scouts/synthesis: 4/2.

## 2026-06-14 17:30 · d4221fce · full leverage audit (session diff 430b35b0..HEAD: enricher, audiobook/discover, frontend, #226) — 4 scouts / 2 synthesis

### TL;DR (full audit)

Mostly tidy. The genuinely high-value, low-risk wins were APPLIED this pass; the two largest "🔴" duplication claims were correctly downgraded by synthesis as false leverage (forcing divergent behavior into one flag-driven blob), and the headline tooling/library swaps were rejected at parity.

### Applied this pass ✅

1. ✅ Dropped `MetadataEnricher.representativeReleaseMbid` + `MusicBrainzClient.searchReleases` + 3 orphan DTOs (`MbReleaseSearch/MbRelease/MbReleaseGroupRef`). It fired a **2nd rate-limited MusicBrainz round-trip per album** to write `album.musicbrainzId`, which is **write-only dead data** (domain `Album` has no such field — `Types.kt:55` is Artist; `LibraryMappers.toAlbum` never maps it). Halves per-album enrichment latency, removes ~40 lines.
2. ✅ Deleted the dead `imageRepo.findByEntityIdAndIsPrimaryTrue?.let{delete}` in `downloadCoverIfMissing` (the guard at the top already returns when a primary exists).
3. ✅ Collapsed `useDiscover`/`useDailyMix` → shared `useRecommendationFeed(queryKey, fetch, limit)` factory; merged service `getDailyMix`/`getDiscover` → private `getRecommendationFeed(feed, limit)`. ~25 lines, behavior-identical (type-check + build green).

### Deferred (worth-it, Medium effort — not applied)

- `<TrackSection>` _presentational shell_ for ExploreNew/DailyMix/FavoriteSongs — extract the shared `<section>`+header+load/error/empty switch ONLY; keep thin per-feature wrappers (their empty-policy, header-action, and data source genuinely diverge). ~110–130 lines (NOT the 170 first claimed).
- Controller `recommendationResponse` + `musicSurfaceFilter` helpers (~12–15 lines; keep the distinct seed-collection loops).
- `wait-backend-rollout` composite action to de-dupe the ~22-line poll across `ci.yml`/`v2-ci.yml`.
- `no-console` → `error` (drop bash hook); drop `bandit` pre-commit hook (ruff `S` already covers it); `include-git-sha: false` for the backend image (dead build-arg).

### Rejected as false leverage / not worth it

- **`ProviderHttpClient`** merging the two enricher HTTP clients — they diverge on body type, retry (loop vs single-shot), redirect/HTTP-version, and timeout; the generic needs 6 policy knobs over ~4 shared lines. REFUTED.
- **detekt re-enable as "drop-in"** — it's a real Kotlin-2.1 / detekt-1.23.7 incompatibility needing a detekt-2.x upgrade + findings triage, not a 4-line deletion. A quality decision, not free leverage.
- **`getRecentAlbums` "dead"** — NOT dead; referenced by a core integration test.
- **`excludeGenres` "5-layer threading"** — that IS the mandated hexagonal port path, not over-engineering.
- **Hand-rolled Levenshtein / RateLimiter / retry** — commons-text/spring-retry/resilience4j not on classpath; net-new dep unjustified for a 1 req/s worker. KEEP.
- **Biome/oxlint/pnpm/tsgo** — parity gaps (no `eslint-plugin-boundaries` equivalent for Core→Platform→Web layering; tsgo beta).

### Total Estimated Savings (full audit)

Applied: ~65 lines removed + one MusicBrainz round-trip/album eliminated. Deferred (if pursued): ~150 more lines + 1 composite action + 2 pre-commit hooks. New deps: none.

Scouts/synthesis: 4/2.

## 2026-07-13 · 7914c594 · full leverage audit — 6 scouts / 2 synthesis (merge + adversarial verify)

### TL;DR (2026-07-13)

A month of fast feature growth (~29k lines since d4221fce) left two dominant debt classes: **config that silently does nothing** (8 independent instances — the worst being that NO dependency bot has run since 2026-07-02, so dependency updates keep accumulating unmerged) and **v1-retirement shrapnel** (dead ports/paths/namespaces that break every out-of-the-box path: `docker compose up` cannot boot, standalone chart install CrashLoops, the Android APK link 404s in prod, the post-deploy QA script polls a nonexistent namespace). On the code side, business logic keeps accumulating in adapters and drifting per protocol (radio ×2, semantic search ×2, recently-played ×2, audiobook predicate ×14). Adversarial verify: 13/14 top findings CONFIRMED, 1 downgraded (Hikari), 0 dropped by the anti-hallucination gate.

### Action list (ranked by impact/effort)

1. **DO 🔴 Restore the dependency bot.** `renovate.json` is a phantom: the Renovate app was never installed (0 renovate PRs/issues ever, no Dependency Dashboard despite the `:dependencyDashboard` preset), and `.github/dependabot.yml` was deleted 2026-07-02 (08919e19, "Single dep bot"). CVE/patch flow fully severed since then. Install the Renovate app OR restore dependabot.yml and delete renovate.json. Gate: first bot PR within one schedule window. Easy.
2. **DO 🔴 Fix the OOTB boot path.** (a) `docker/docker-compose.yml:64` healthcheck uses `curl` but the backend image (`yay-tsa-v2/Dockerfile:61-67`) ships only `wget` → backend never healthy → frontend (`depends_on: service_healthy`) never starts; mirror the Dockerfile's own `wget -q --spider` HEALTHCHECK. (b) `apps/web/docker/entrypoint.sh:221,225-226` fallback URLs point at retired v1 (`backend:8096`, `yay-tsa-backend.yay-tsa-production.svc:8096` — namespace doesn't exist, kubectl-verified) → standalone `charts/yay-tsa` install (default `backendUrl: ''`) CrashLoops nginx. Delete fallbacks, fail fast. Gate: `docker compose up` reaches healthy + frontend serving. Easy.
3. **DO 🔴 Karaoke hardening: cache-hit NPE burns the retry budget.** `KaraokeProcessor.kt:128` calls `Path.of(result.vocalPath)` where `vocalPath: String?` (`SeparatorClient.kt:14,52` maps `""`→null; `app.py:265-267,318-322` legitimately returns `""` for instrumental-only cache hits) — platform-type NPE exactly when a valid instrumental EXISTS → `recordFailure` ×3 → track terminally FAILED. Treat null vocalPath as instrumental-only success. Gate: integration test with cached instrumental-only asset. Easy.
4. **DO 🔴 Make sonar config real.** `sonar-project.properties:1` declares `nikolay-eremeev_yay-tsa` — that project does not exist (live API); the real one is `nikolay-e_yay-tsa` (automatic analysis). Server-side exclusions still contain `yay-tsa-v2/**` while the file's comment claims v2 "is no longer excluded"; live ncloc has ZERO Kotlin; every multicriteria suppression in the file is inert; the "detekt is disabled (issue #267)" comment is stale (detekt re-armed). Fix projectKey, reconcile the v2 exclusion, update comments. Easy + triage.
5. **DO 🔴 Fix or delete `tools/post-deploy-qa.sh`.** Defaults (`:5-7`) target nonexistent `yay-tsa-production` ns / `yay-tsa-production-backend` deploy (real: `yay-tsa` / `yay-tsa-v2-production-backend`, kubectl-verified); Phase 5 silently reports "0 errors" from the missing deployment; `:62` `${PROD_URL}/api/manage/health` → live 404 (actuator on port 9090). Verifier found NO caller passes correcting env (Argo autoqa runs its own `run-all.sh` with inline rollout-wait; `/qa` references the CI job name, not this script) — it is orphaned AND broken. Gate: grep invokers once more; then either fix defaults + health URL and wire it in, or delete. Keep the auth round-trip if fixing (standing rule: never strip auth from post-deploy checks). Easy.
6. **VERIFY 🔴 (gate: product decision + `curl -sI https://yay-tsa.com/downloads/yay-tsa.apk` → 200 or link gone)** — "Download Android App" is a live 404: CI uploads the APK as a 1-day artifact only; nothing ships it into the nginx image. Either wire the APK into the image (Medium) or drop `SettingsPage.tsx:278` + `nginx.conf.template:165-170` (Easy). Side-finding for a separate look: the CSP served on yay-tsa.com (`unsafe-inline unsafe-eval`) is NOT nginx's hash-based CSP — an upstream layer overrides it.
7. **DO 🟡 Fix the dead Hikari block** (downgraded from scout's 🔴): `application-prod.yml:17-22` nests `datasource.hikari` under `springdoc:` (YAML blank line doesn't reset nesting) → block dead. Impact is small — pool size 10 still arrives via `HIKARI_MAX_POOL` env (chart configmap:15-16 → base application.yml:27-28); only connection-timeout (20s vs intended 30s) and idle-timeout (300s vs 600s) differ. Dedent or delete. Gate: hikari metrics after deploy. Easy.
8. **DO 🟡 Move recommendation logic out of adapters into `core-application/recommendation` (one coherent move).** MCP radio (`McpTools.kt:540-556`) is a second, diverged algorithm (no diversification/affinity-rerank; verifier: MusicSurfaceFilter IS applied, so no red-line leak) while `RadioStationService.kt` (189 lines, zero jellyfin imports, comment "exactly one radio algorithm") sits unreachable in adapter-jellyfin; ~220 lines of Daily-Mix/Discover funnels in `JellyfinAdaptiveController.kt:456-673`; semantic-search fallback duplicated (`JellyfinAdaptiveController.kt:432-449` vs `McpTools.kt:225-300`, the MCP copy already perf-diverged with N×`getTrack`); recently/most-played implemented twice with silently diverged params (`SubsonicController.kt:363-396` vs `JellyfinItemsController.kt:555-587`). Free follow-up: Subsonic similar/top-songs gain musicSurfaceFilter. ~450 lines relocated, 4 duplicate algorithms → 1. Gate: `./gradlew build` + adapter integration tests + MCP start_radio smoke. Medium (2-3 days).
9. **DO 🟡 Collapse the audiobook predicate: 14 sites → 1.** 5 Kotlin literal sets (`YaytsaAudiobooksController.kt:152`, `MusicSurfaceFilter.kt:81`, `LlmOrchestrator.kt:63`, `LibraryWriter.kt:46`, `MetadataEnricher.kt:58`) + 9 SQL `lower(g.name) IN ('audiobook','audiobooks')` fragments (`LibraryEntityRepository.kt:149,175`, `KaraokeAssetJpaRepository.kt:20`, `PlayHistoryJpaRepository.kt:67,92,122`, `TrackFeaturesJpaRepository.kt:23`, `AffinityAggregator.kt:93,180`) — while the same `LibraryEntityRepository` already passes `:excludedGenres` as a bind param at :196,226. One shared constant + bind param (or a Flyway view). A missed site = silent audiobook leak (the exact bug class fixed 2026-07-12). Gate: `grep -rn "'audiobooks'" --include='*.kt'` count = 1 + exclusion integration tests. Medium.
10. **DO 🟡 Un-blind knip and harvest what it hides.** `knip.json` ignores `features/**/stores/*.ts` + `**/hooks/*.ts` → CI `check:unused` false-greens over 15 verified-dead symbols (2 dead barrel files; `auth.store.ts:330-333`, `player.store.ts:2047-2049`, `playback-timing.store.ts:56-57`, `useAlbums.ts:17,50`, `useArtists.ts:16,70`, `local-resume.ts:72 clearLocalResume` — dead ⇒ audiobook local-resume entries never cleaned). Note: web `useRecentAlbums` is dead; core `ItemsService.getRecentAlbums` is a different layer and stays LIVE (no contradiction with June). Also stale: schema pinned knip@5 (installed 6), ignore for nonexistent `lighthouserc.js`, masking `ignoreDependencies` (jsdom). Gate: `npm run check:unused` green WITHOUT the ignores + type-check + build. Easy.
11. **DO 🟡 Frontend dedup batch:** (a) single SSE connection — `RootLayout.tsx:71-72` mounts `useDeviceEvents` + `useRemoteCommands`, each opening its own `EventSource` to `/v1/me/devices/events` with byte-identical reconnect blocks (`useDeviceEvents.ts:70-97` ↔ `useRemoteCommands.ts:194-218`; third resync copy `group-sync-store.ts:205-214`; degraded warnings fire twice) — ~50-70 lines + halves per-client SSE connections (Medium); (b) color-extraction algorithm duplicated whole between main thread and worker — 87 lines, largest jscpd clone — extract shared pure module (Easy); (c) `PlayPauseIcon` ×3 (Easy). Gate: build + E2E player/device-sync specs + network tab shows 1 SSE connection.
12. **DO 🟡 Enricher-local `ProviderHttp`:** `ItunesCoverClient.kt:106-127` ↔ `DeezerCoverClient.kt:102-123` byte-identical (handleStatus/extensionFor/getString/downloadImage), CoverArt `extensionFor` third copy — ~150 lines. Cross-module generic client stays REFUTED (LlmClient/SeparatorClient genuinely diverge). Gate: gradle build + enricher integration test. Medium.
13. **DO 🟡 Close two worker config-drift hazards:** `KaraokeProcessor.kt:55 MAX_FAILURES=3` hardcoded vs `JellyfinKaraokeController.kt:30` `@Value fail-threshold` (env changes the API report, not the worker); 16-entry cover-filename list duplicated with a "keep the two lists in sync" confession (`LibraryWriter.kt:48-66` ↔ `MetadataEnricher.kt:378-397`). Easy.
14. **DO 🟡 `.dockerignore`:** root-anchored `node_modules/` leaks ~133MB of nested node_modules into the build context AND copies them over fresh `npm ci` (local version-skew hazard); dead v1-era entries (helm/, k8s/, server/target/, .travis.yml, .gitlab-ci.yml, Dockerfile.dev, test-media/); missing test-results/, playwright-report/, tools/. Fix: `**/node_modules` + cleanup. Easy.
15. **DO 🟡 Helm cleanup:** `_helpers.tpl` verbatim-identical ~50 lines ×2 charts (both already depend on yay-tsa-common); TLS loop inlined in 2 PWA ingresses while the common helper serves all 4 v2 ingresses; deprecated `kubernetes.io/ingress.class` annotation alongside `ingressClassName`; dead `charts/yay-tsa/values.yaml` knobs — `serviceAccount` (0 refs), `podSecurityContext`/`securityContext` (templates hardcode both — overrides silently no-op), `autoscaling.enabled` (no HPA template exists). ~190 lines + 3 lying knobs. Gate: `helm template` diff-identical. Easy-Medium.
16. **DO 🟡 Delete the dead X-Accel media path (v1 leftover):** `nginx.conf.template:5` thread_pool, `:229-249` `/_internal/media/`, `YAYTSA_MEDIA_PATH` plumbing, web Dockerfile `/media` mkdir — 0 `X-Accel` hits in v2 Kotlin. ~40 lines. Gate: container boots + streams. Easy.
17. **DO 🟡 `docker/example.env`:** delete the METADATA PROVIDERS section (GENIUS/LASTFM/SPOTIFY/ACOUSTID — zero consumers anywhere; the enricher is keyless) and document required `KARAOKE_ENABLED=true` (profile alone runs the GPU sidecar with karaoke still off). Easy.
18. **DO 🟡 Backend quick wins:** N+1 in `trackToSeedDto` (`JellyfinAdaptiveController.kt:687-691` — two `getEntityNamesByIds(setOf(it))` per track, up to 200 queries/search; batch like `recommendationResponse:413`); name-lookup loader written 6× → `TrackLookups.load()` in adapter-shared (~35 lines); MPD `commands` list hand-maintained parallel to the dispatcher and already drifted (`decoders` handled at :165, missing from list) — derive from one map. Easy each.
19. **DO 🔵 DELETION BATCH** (one pass, shared gate: `npm run build` + type-check + `check:unused` + `./gradlew build`): root `Makefile` (63 lines, verifier-confirmed unreferenced — Argo builds via buildkit directly, no `make` anywhere; C-scout's "CI entrypoint" claim was the Makefile quoting itself); `.github/actions/wait-backend-rollout` (orphaned since 98da24aa); ci.yml dead Java setup step in the pre-commit job (~15-30s/run) + `develop`/`v*` triggers; `npm install -g @lhci/cli` → `npx` (devDep already pinned); 3 package.json scripts referencing nonexistent files (`generate:splash`, `sync:version`, `postversion`) + broken `apps/web/scripts/generate-icons.js` (reads nonexistent svg) — **sharp STAYS** (live consumer `scripts/profile-images.mjs:12`); root `jsdom` devDep (apps/web has its own); jscpd inline-config duplication + Svelte-era leftovers (.jscpd.json svelte format, `.gitignore` .svelte-kit/target); `mise.toml` maven (no pom.xml anywhere); `.gitignore` += `.diffctx/`, `sourcemaps-out/`; dead `IndexedDbOfflineStore` methods (hasTrack/deleteCover/clearAll/close, ~30 lines); `TICKS_PER_MS` redecl (`useAudiobooks.ts:8`), `formatDuration` reimpl (`AudiobooksPage.tsx:27-35`), `toError` idiom ×9, UUID-fallback ×3 (export core's), `getPlaybackUrl` async no-op (`offline.store.ts:469-476` + 4 call sites), `isEvictableCacheEntry` inline copy (`OfflineManager.tsx:84-88`), dead `VITE_YAYTSA_DEVICE_ID` decl, orphan `favicon.png`, 4 unreferenced icon PNGs precached by workbox on every install; dead tick-helper trio in adapter-jellyfin `JellyfinDtos.kt:51-55` (+ rename to JellyfinAuthDtos.kt — two same-name files trap); dead `yaytsa.media.root-path` key + `MEDIA_ROOT_PATH` env ×2 compose files (verifier: zero Kotlin readers; `music-path` has 5+); orphan `scripts/generate-test-media.sh`; `jellyfin-api-spec.json` (1.7MB largest tracked blob, referenced only by 5 ignore-configs) + those 5 ignore entries; `FEATURES.md` (stale AC-registry whose traceability claim greps to 0 matches; fix `FEATURES_OVERVIEW.md:5` pointer) + `docs/media-session-lockscreen.md` + `docs/perf-profile.md` (one-off dumps, 0 inbound refs); Spleeter from 3 docs (0 code hits — real backends are BS-Roformer + Demucs); requirements/\*.txt → base.txt + `-r` (drifted floors, cu121 vs cu124); MlFeatureExtractor KDoc "what"-docstrings. ≈ 450 lines + 1.7MB + ~45KB docs.
20. **DO 🔵 Stale-docs batch:** regenerate `yay-tsa-v2/docs/generated/*` (2026-04-18 — predates infra-metadata-enricher + core-application:recommendation, both absent from the graph) or wire the gradle task into CI; fix the namespace story (real topology = single ns `yay-tsa`, kubectl-verified) in `charts/yay-tsa NOTES.txt:14`, root `CLAUDE.md:161-162`, `charts/yay-tsa-v2/values.yaml:227-228` stems comment (cross-ns rationale gone — shared PVC may now be possible), `charts/yay-tsa/ingress.yaml:1-4` header; v2 CLAUDE.md "PostgreSQL 16" → 17 + add enricher to the workers list; date-stamp `perf/PERF_REPORT.md` (keep the harness — reproducible, tied to the /Items p95 target); e2e README paths; archive `scripts/backfill-play-history-2026-07.sql` once the prod run is confirmed (do NOT convert to a migration). Easy.
21. **DO 🔵 Still-open from June (2nd reminder):** move knip/jscpd/type-coverage hooks to `stages: [pre-push]` (June's "highest-ROI single move", still unapplied) and replace the `component-naming` grep hook. Patch pins: detekt 1.23.7→1.23.8, spotless 7.0.2→7.0.4 (registry-verified). Easy.
22. **DO 🔵 Chart portability:** `charts/yay-tsa-v2/values.yaml:169,174` bake author release/namespace names (`yay-tsa-production-secrets`, embedding-server FQDN) into defaults shipped to umbrella users — move to gitops values, default empty. Add application.yml env-indirection for `yaytsa.metadata.{itunes,deezer,cover-*}` keys (METADATA\_\* convention gap). Easy.
23. **VERIFY 🔵 (gate: same-image import + fixture test where duration-mismatched lyrics get rejected)** — unify the two Python LRCLIB fetchers: `lyrics_sync.py:86-115` skips `_validate_lyrics`, so the nightly cron can write synced-but-wrong-duration LRC that `app.py:665-789` would reject. Kotlin LrclibClient is a different runtime — leave it. Medium.
24. **VERIFY 🔵 (gate: reproduce starvation in an integration test — enricher cycle + outbox concurrently on pool.size 4)** — long-running `@Scheduled` bodies (MetadataEnricher minutes/cycle; MlFeatureExtractor 600s/track) run inline on the shared 4-thread scheduler; only KaraokeProcessor has the dedicated-executor pattern. Mitigated in prod (`ML_ENABLED=false`). If reproduced, extract the karaoke executor+guard as `SerialBackgroundWorker`. Medium.
25. **VERIFY 🔵 (gate: confirm no local workflow uses `--target development`/`test`)** — apps/web/Dockerfile unreferenced stages (development, playwright test — Argo builds only `production`) + unused base-stage `apk add bash git curl wget`. Easy after confirmation.
26. **VERIFY 🔵 (gate: product decision — document as intentional or add a scheduled job)** — live-backend Playwright projects (chromium/mobile/webkit, ~20 specs) have no CI gate in this repo; GH CI runs only `chromium-mocked`; live specs run only via external autoqa. Facts confirmed; whether it's a gap is a product call.
27. **DON'T (WIP by design — convert before next feature growth, not as audit cleanup)** — groups feature on raw JdbcTemplate in the composition root (`app/.../groups/GroupSyncService.kt`, 313 lines, bypasses OCC/idempotency/outbox).
28. **DON'T (exonerated)** — `player.store.ts` split: siblings already extracted; the karaoke slice is entangled with shared closure state — extracting it as a store is a leakier seam. Karaoke stays (standing rule).
29. **DON'T (bodies genuinely diverge)** — FavoritesPage section wrapper; items.service query-tail helper only if touching the file.
30. **DON'T (standing parity rejections re-confirmed)** — Biome/oxlint/pnpm/tsgo swaps; cross-module ProviderHttpClient; watermark/batch-loop merge across workers (different consistency semantics); client-telemetry dedup (canonical copy-out module by design); React.memo bulk-removal (PerfMemo.test guards it); ML orphan filter (consumers already drop orphans).

### Systemic Patterns (2026-07-13)

1. **v1-retirement shrapnel.** The v1 backend is gone but its ports/paths/namespaces aren't: entrypoint 8096 fallbacks, the X-Accel media path, Svelte/Maven ignore entries, the namespace story in docs. A retirement should end with a grep for the retired component's names, ports, and paths.
2. **Config that silently does nothing (8 instances).** renovate.json without the app; sonar projectKey typo; Hikari under `springdoc:`; `media.root-path` with zero binders; helm securityContext/serviceAccount/autoscaling ignored by templates; example.env keys for a keyless enricher; `KARAOKE_FAIL_THRESHOLD` changing the report but not the behavior; post-deploy-qa defaults aimed at a nonexistent namespace. Nothing validates that config is _consumed_; the failure mode is always silent false confidence.
3. **Detectors blinded by their own config.** knip ignores hide 15 dead symbols behind a green gate; sonar's file-vs-server divergence means zero Kotlin is analyzed while the file claims otherwise. (Done right: vulture's maintained whitelist — 0 false negatives.)
4. **Business logic accumulates in adapters, then drifts per protocol.** Radio ×2, semantic search ×2, recently-played ×2, recommendation funnel in a controller, audiobook predicate ×14, name-loader ×6 — every duplicate has already diverged in behavior or perf. `core-application/recommendation` exists as the home; actions 8-9 are one coherent move.
5. **OOTB paths ship broken because nothing exercises them.** Compose can't boot, standalone chart CrashLoops, APK link 404s, the QA script polls a nonexistent namespace. The internally-deployed path is healthy; every path only an outside user walks is broken. A minimal "compose up + curl" CI job would catch the entire class.

### Self-contradiction check vs 2026-06-14

- ⚠ detekt: was "real Kotlin-2.1 incompatibility needing detekt-2.x, not free leverage" (17:30 entry — itself contradicting the same day's 14:30 "1.23.8 is compatible"), now **re-armed on 1.23.7** with 26 baselines + classpath pin (`build.gradle.kts:51-59`). The "needs 2.x" verdict was wrong; residual = patch bump.
- ⚠ lodash 4.18.1: was "phantom version, likely typo", now registry-verified **current latest**. June claim factually wrong; closed.
- ⚠ Spotless: was "8.6.0 out", now registry-verified latest **7.0.4**. June version claim wrong.
- ⚠ CoverArt retry: was "latent reliability gap, always-yes fix", now **superseded by design** — transient → `MetadataProviderUnavailableException` → 6h `parkCoverAttempt` loop-level retry. Do not re-add inner retry.
- ⚠ wait-backend-rollout composite action: June said "create it to dedupe ci.yml/v2-ci.yml"; it was created, then orphaned within a month (98da24aa moved QA to Argo). Verdict inverted to delete.
- ⚠ `getRecentAlbums` "NOT dead" (June): still true for core `ItemsService.getRecentAlbums` (integration-test consumer); the **web hook** `useRecentAlbums` is a different symbol and IS dead. No flip — layer distinction recorded to prevent one.
- Meta: June's log contains an intra-day self-contradiction (detekt) and two wrong version claims — registry/API verification before any pin-related verdict is now standard (this pass verified npm, Maven Central, SonarCloud, GitHub API, and the live cluster).

### What I didn't touch

Group Sync internals beyond the layering note (WIP); `yay-tsa-v2` dependency majors (Kotlin 2.1→2.4, Spring Boot 4) — separate bump effort as before; the CSP-override side-finding (needs a gitops/Cloudflare look); ANALYSIS\_\*/PLAN.md scratch (gitignored by design).

### Total Estimated Savings (2026-07-13)

- Lines deletable: ~950–1,100 across ~25 files + 1.7MB blob + ~45KB stale docs
- Abstractions collapsed: 2 (enricher ProviderHttp-local, shared SSE hook); 0 new modules (moves reuse `core-application/recommendation`)
- Duplicate implementations removed: 4 algorithms → 1, audiobook predicate 14 → 1, name-loader 6 → 1, SSE reconnect 3 → 1, color-extraction 2 → 1, LRCLIB 3 → 2
- Dependencies added: 0
- Dependencies removed: 1 (root jsdom); sharp retained (verified live)
- Config files removed: ~6 (Makefile, wait-backend-rollout, FEATURES.md, api-spec, 2 docs/) + 8 silently-dead config blocks fixed
- Tools replaced: 0 (standing rejections hold); gates un-blinded: 2 (knip, sonar); restored: 1 (dep bot); made passable: 1 (post-deploy-qa)
- Estimated CI-time reduction: ~15–30s/run + lhci download + 3 full-repo scans commit→pre-push; Docker context −133MB
- Files moved: ~5 Kotlin files adapter → core-application
- Overall migration effort: deletion batch + config fixes ≈ 1 day; recommendation move + audiobook predicate ≈ 2–3 days; rest piecemeal-easy

Scouts/synthesis: 6/2 (merge + adversarial verify; 13 CONFIRMED, 1 downgraded, 0 hallucination-dropped).
