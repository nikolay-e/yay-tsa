# QA — yay-tsa

Project-specific QA playbook for yay-tsa: a Kotlin/Spring Boot multi-protocol media server (Jellyfin + OpenSubsonic + MPD + MCP over one state engine) paired with a React PWA client. Generic methodology lives in the global `/qa` skill — this file holds only what is unique to yay-tsa.

## Applicability Matrix

| Capability                     | Applies | Notes                                                                                                                              |
| ------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| CI                             | ✅      | `ci.yml` (PWA + post-deploy-qa), `v2-ci.yml` (Kotlin backend build)                                                                |
| CD / ArgoCD                    | ✅      | Image Updater write-back; two namespaces (see Coordinates)                                                                         |
| K8s logs                       | ✅      | Backend, frontend, audio-separator, feature-extractor CronJob                                                                      |
| Browser QA (Playwright)        | ✅      | Most click-heavy UI in the fleet — see `walkthrough.safe_click`                                                                    |
| Backend smoke                  | ✅      | Read + binary (Range/206) + write — never trust `/health` alone                                                                    |
| Tests                          | ✅      | Gradle (Testcontainers), `packages/core` integration, Playwright E2E                                                               |
| Diff-context review            | ✅      | `diffctx . --diff <from>..<to>`                                                                                                    |
| autoqa full                    | ✅      | crawler + axe + schemathesis + ZAP                                                                                                 |
| Schemathesis                   | ✅      | OpenAPI at `/api/v3/api-docs`, base-url `https://yay-tsa.com/api`                                                                  |
| ZAP                            | ❌      | Off in the in-cluster autoqa (`zap-enabled: "false"` in gitops sensor): the autoqa image runs ZAP via Docker, no daemon in-cluster |
| SonarCloud                     | ✅      | Automatic Analysis (see SonarCloud section)                                                                                        |
| Chat-review (LLM-authored msg) | N/A     | No user-facing bot/agent messages; LLM-DJ output is queue edits, not user-facing text (wired to LiteLLM, gated on `LLM_ENABLED`)   |
| Walkthrough Delta Pass         | ✅      | Long lists must be scrolled ≥30 items before screenshot                                                                            |

## Coordinates

- **Public URL**: `https://yay-tsa.com` (Subsonic at `/rest/*`, API at `/api/*`).
- **Namespaces**: `yay-tsa` (current prod — backend, frontend, audio-separator, feature-extractor pods all live here) plus legacy `yay-tsa-v2-production` references in some charts/scripts. CNPG cluster in `shared-database`.
- **OpenAPI**: springdoc default `/api/v3/api-docs` (NOT Jellyfin's legacy `/api-docs`). schemathesis `--url https://yay-tsa.com/api`.
- **SonarCloud key**: `nikolay-e_yay-tsa` (org `nikolay-e`). The `qualitygates`/`issues` APIs 404 on any other key — verify with `api/components/search?organization=nikolay-e&q=yay-tsa`.
- **QA account**: username `test`; password in Keychain entry `yay-tsa-qa-password`. Subsonic `/rest/*` uses query-param auth (u/p/t), so a Bearer token 401s there — expected. NEVER use the `master` account (owner/admin; browser may autofill it) — admin login is forbidden during `/qa`.
- **DB schemas**: per-context `core_v2_*` (`auth`, `library`, `playback`, `adaptive`, `preferences`, `playlists`, `ml`, `karaoke`, `shared`) in `yaytsa_production`. `public` holds only extensions (pgvector, pg_trgm, citext, uuid-ossp).

## Repo topology correction (origin = Forgejo, GitHub = mirror)

Despite the project `CLAUDE.md` line claiming `origin` is `github.com:nikolay-e/yay-tsa`, the real `origin` is **Forgejo** (`git.nikolay-eremeev.com/nikolay-e/yay-tsa`) and GitHub is the push-mirror. Push fixes to `origin` (Forgejo); the mirror propagates to GitHub fast (CI on the GitHub side fired within ~seconds this pass). `gh` still works because it targets the GitHub mirror — issues, PRs, and `gh run` are all on GitHub. Dependabot PRs (`#242`, `#243` this pass) open + auto-merge on GitHub, land on GitHub `main`, and sync back to Forgejo — so a local `main` based on a pre-merge SHA shows up as "ahead N, behind M": rebase onto `origin/main` before pushing. Merging a dependabot PR via `gh pr merge` is correct here.

## Schemathesis residual is now an autoqa gate-policy fix (not app-fixable)

The chronic `post-deploy-qa` red — N× `[400] Undocumented Content-Type: text/html` on fuzzer-mutated paths containing C1 control chars (`%C2%92`, `%C2%82`) — is Tomcat's connector rejecting the URI **before** Spring's RFC7807 handler. Reproduce: `curl -X DELETE 'https://yay-tsa.com/api/Admin/Users/_V%3E%C2%92%23%5B%C2%82_'` → Tomcat HTML 400, while `.../foo%3Bbar` → `application/problem+json` (the StrictHttpFirewall/semicolon class is fixed). Unfixable in app code. As of **nikolay-e/autoqa@57bd544** the gate classifies a schemathesis failure whose sole finding is `text/html` content-type on a **4xx** as informational (not blocking); 5xx text/html still blocks, and an unreconciled parse falls back to failing the full count. So this class should no longer gate `main` — if post-deploy-qa is red again, confirm it's NOT this class before triaging. Don't add these endpoints to `schemathesis-exclude-paths` (kills real positive coverage).

## autoqa integration

Full pipeline: crawler + axe + schemathesis + ZAP, in the `post-deploy-qa` job of `ci.yml`. **Budget `timeout-minutes: 120`** — schemathesis `--checks all` runs three phases (Coverage ~8min + Fuzzing ~30min + Stateful ~25min) sequentially BEFORE crawler/axe/ZAP, and the chronic failing endpoints make Hypothesis shrink for a long time; at 60min the job was SIGTERM'd (`exit 143`) mid-Stateful and ZAP/crawler never ran. The autoqa step runs schemathesis first, so a truncated job means NO ZAP/crawler/axe results — check the job ran to a `Complete job` line, not just that the summary counts look plausible. See `/qa` skill `Autoqa Logs` + `Crawler / autoqa` for log-reading and false-positive triage. yay-tsa-specific autoqa inputs already wired in CI: `schemathesis-base-url: https://yay-tsa.com/api` (Traefik strip-prefix means backend OpenAPI emits bare paths), and `schemathesis-exclude-paths` covering the binary image endpoint + SSE/event streams (`/v1/me/devices/events`, `/v1/me/devices/commands`, `/v1/groups/{groupId}/events`, `/Karaoke/{trackId}/status/stream`). The brace name MUST match the springdoc path template exactly — schemathesis `--exclude-path` is a literal template match, so `/v1/groups/{id}/events` excludes nothing (real param is `{groupId}`) and the SSE endpoint gets fuzzed into false conformance failures.

## walkthrough.safe_click

This is the most click-heavy UI in the fleet. The crawler clicks only the verbs below; everything else is left unclicked.

```yaml
walkthrough:
  safe_click:
    [
      Open,
      Expand,
      Show more,
      Filter,
      Sort,
      Play,
      Pause,
      Resume,
      Next,
      Previous,
      Seek,
      Shuffle,
      Repeat,
      Mute,
      Unmute,
      Favorite,
      Unfavorite,
      Queue,
      Lyrics,
      Karaoke,
      Search,
    ]
  # Destructive / state-mutating controls to AVOID (not on allowlist):
  #   Logout, Delete playlist, Remove from playlist, Clear queue,
  #   Library scan / rescan, Delete device, Leave group, Save preferences,
  #   any /Admin/* control, "Remove from favorites all", PWA uninstall.
  confirm_dialog_detected: fail # any unguarded confirm modal mid-walk fails the run
```

## Mandatory Browser-QA flows (project-specific positive signals)

Health endpoints, `/Items` HTTP 200, no-pod-restart, "the page renders" are liveness only — the audio-stream 500 shipped with all four green. Run on BOTH desktop and mobile (412×915, Pixel 7); each flow needs 0 console errors, 0 unexpected 4xx/5xx, and the named positive signal.

1. **Playback start (most critical)** — open album → Play first track. Within 3s: `<audio>` exists, `paused===false`, `currentTime>0`, `GET /api/Audio/<id>/stream` returns `206` (range) or `200`. Console must NOT contain `MEDIA_ELEMENT_ERROR` / `Failed to load audio` / `Audio engine error` (backend non-audio body the element can't decode).
2. **Seek** — `audio.currentTime = duration*0.5`; within 2s network shows fresh `Range: bytes=<offset>-` → 206. Repeated byte-0 request = range support broken.
3. **Next/previous** — Next fires new `/stream`, `currentTrack.Id` changes. Previous <3s in → previous track; >3s in → seeks to 0. Both required.
4. **Favorites** — heart → `POST /api/UserFavoriteItems/<id>` 200, icon fills, `/api/Items?Filters=IsFavorite` includes it; click again → DELETE 200.
5. **Karaoke** — toggle loads `/api/Karaoke/<id>/instrumental` 200; vocals toggle switches `/instrumental` ↔ `/vocals`.
6. **Devices / multi-device SSE** — `new EventSource('/api/v1/me/devices/events')` opens (readyState 1), receives `ready` within 5s; second tab appears within ~15s (heartbeat). SSE 500 = auth filter chain stripped the cookie (distinct from streaming 500).
7. **Radio similarity (ML actually fires)** — start a session, `POST /api/v1/sessions/{id}/queue/refresh`, then `fetch('/api/v1/sessions/{id}/queue').then(j => j.tracks.map(t => t.addedReason))` must contain at least one `bootstrap-similarity`. If none, the embedding path is dead (HNSW disabled, MERT column NULL, or controller bypassing `findSimilarTracks`). Audio-playing alone does NOT distinguish similarity-driven radio from generic top-affinity fallback. **Before declaring the path dead, confirm the seed actually registered.** The `POST /api/v1/sessions` body field is snake_case `seed_track_id` with NO PascalCase/camelCase alias (`StartSessionRequest.seed_track_id`) — sending `SeedTrackId`/`seedTrackId` parses to null, the session has no seed, and `findSimilarTracks` is never called → queue is 100% `bootstrap-affinity` (a TEST ARTIFACT, not a bug). A correct seed yields `seed-track: 1` + `bootstrap-similarity: N`. A seed whose own track has no embedding (e.g. a 20-min full-album FLAC the ML extractor skipped) correctly falls back to `seed-track: 1` + `bootstrap-affinity: N` — graceful, also not a bug. Test ≥3 distinct real seeds before concluding; one embedding-less seed proves nothing.

For login/seek/auth-injection mechanics, mobile locator state-changes, PWA service-worker cache, Range-aware mocks, and the binary-path rule — see `/qa` skill `Browser QA`.

## Project-specific gotchas

### Multi-protocol path catalog

- **PWA → Jellyfin/Yaytsa**: `/Users/AuthenticateByName`, `/Sessions/Logout`, `/Items`, `/Items/{id}/Images/{type}`, `/Audio/{id}/stream`, `/Playlists`, `/UserFavoriteItems/{itemId}`, `/Sessions/Playing[/Progress|/Stopped]`, `/Admin/Users`, plus extensions `/v1/me/devices/*`, `/v1/sessions/*`, `/v1/users/{id}/preferences`, `/Karaoke/{trackId}/{status|instrumental|vocals}`.
- **Subsonic** lives at `/rest/*`, NOT `/api/rest/*` (class-level `@RequestMapping("/rest")`). **The real edge on `yay-tsa.com` is the frontend nginx, not the dedicated Traefik subsonic ingress.** Traefik routes the whole host to the frontend `yay-tsa-production` ingress (its length-based default priority beats the subsonic ingress's explicit `router.priority: "20"`), so `/api` works only because nginx proxies it (`location /api/`) and `/rest/*` returns the PWA `index.html` (200 `text/html`) unless nginx ALSO proxies it. Fix = add `location /rest/` (proxy to backend, NO `/api` strip, keep Range for `/rest/stream.view`) in `infra/nginx/nginx.conf.template`, mirroring the `/api/` block. Verify: `curl 'https://yay-tsa.com/rest/ping.view?u=test&p=<pw>&f=json'` must return `{"subsonic-response":{"status":"ok"...}}`, not HTML; `cf-cache-status: DYNAMIC` confirms it's origin (not CF cache). The backend itself is fine — port-forward `svc/yay-tsa-v2-production-backend:80` and hit `/rest/ping.view` to isolate routing from the adapter. schemathesis tests only `/api`, so it never catches a broken `/rest`.
- **MPD** enabled in-cluster only (`MPD_ENABLED=true`, ClusterIP `*-mpd:6600`, unauthenticated → no public ingress). Verify by grepping backend startup log for `MPD server listening on port 6600` — the `/dev/tcp` probe fails inside JRE-Alpine (busybox `sh` lacks it).
- **`/v1/*` extensions** test via the `/api/` prefix (e.g. `/api/v1/time`); the bare `https://yay-tsa.com/v1/*` hits frontend nginx and returns `index.html`. Traefik strip-prefix removes `/api` before the backend sees it.
- **`PlaylistItemId` is the playlist POSITION on the wire** (not a track id). The Move endpoint accepts position-first with track-id fallback; protocol clients (Subsonic/Jellyfin) call `POST /Playlists/{id}/Items/{PlaylistItemId}/Move/{n}` — the PWA never does.
- **Auth surfaces**: only `Authorization: Bearer <token>` survives the Cloudflare/Traefik proxy chain; `X-Emby-Token`, `X-Emby-Authorization`, and `?api_key=` all 401 through the public URL but succeed via `kubectl port-forward`. The PWA sends Bearer for `fetch()` and writes a `yay_token` cookie for `<audio src>` and `EventSource` (neither can set headers). When code authenticates via port-forward but fails via public URL → bug is in the proxy chain.

### Karaoke / audio-separator stem paths

- Stem path pattern: `/app/stems/{trackId}/{filename}_instrumental.wav` and `_vocals.wav`. The stems PVC must be mounted in BOTH the audio-separator AND backend deployments — separator writes, backend reads. If the backend can't see the files, `getStatus()` → `validateStemFilesExist()` resets `karaokeReady=false`.
- `tryRecoverFromOrphanedFiles()` can rebuild DB state from existing stems without re-processing. `karaoke_fail_count` column skips a track after 3 failures (no infinite backfill retries).
- Production uses the GPU sidecar (`services/audio-ml/`, BS-Roformer default); the in-process Demucs path is disabled via `DEMUCS_COMMAND=unsupported`. ML feature extraction is a CronJob on the essentia image (`extractor_cli.py`), NOT in-backend — keeps Python+torch+Essentia out of the JVM image.
- **Manually-pinned CronJob images can ship a broken image silently.** `feature-extractor`/`lyrics-sync`/`taste-clusters` CronJobs share one `services/audio-ml` image, pinned by hand in gitops `values.images.yaml` (Image Updater can't track CronJobs — no live pod to inspect). The three CLIs `import db` (shared `db.py`); if the `Dockerfile` `COPY` list omits a shared module (`db.py`), every CronJob crashes at import (`ModuleNotFoundError: No module named 'db'`) and ML/lyrics/clusters die silently — `/Items` and playback stay green. **Detect every pass:** `kubectl get pods -n yay-tsa` for CronJob pods in `Error`; the audio-separator (`uvicorn app:app`, no `import db`) survives, masking it. **Fix:** add the `COPY` in the Dockerfile, push (rebuilds the image), bump the gitops pin to the new SHA. **Verify the fixed image end-to-end without waiting for the daily schedule:** `kubectl create job -n yay-tsa qa-verify --from=cronjob/yay-tsa-production-taste-clusters` (lightest, no external deps) → pod must reach `Completed` and log real work, not crash at import. Delete the verify job after.
- **ArgoCD app health can wedge `Degraded` after a CronJob job-pod failure** even once the image is fixed and all Deployments/pods are `Healthy` and a fresh job `Completed`: `health.lastTransitionTime` stays frozen at the crash time and does NOT clear via `refresh=hard`, a manual sync, or a successful job. No per-resource health shows `Degraded` (all empty), and there's no custom CronJob health in `argocd-cm` — it's a stale computed field. It clears on the next real resource transition (e.g. the next frontend image roll). Do NOT restart the `argocd` application-controller during `/qa` (admin namespace, forbidden). Trust the live signals (site 200, playback, deployment `Available=True`) over the app's stale `Degraded`.

### JVM heap = 75% of container limit → OOM kills streams

- The entrypoint computes JVM heap as 75% of the container memory limit, so raising the container limit automatically raises heap. When the backend OOMKills, ALL active stream connections break mid-playback — this is the "sometimes plays, sometimes doesn't" symptom.
- Concurrent `/Images/Primary` (50+) each decode a full image to `BufferedImage`, resize, convert to WebP — exhausts heap. Mitigations in place: `MAX_CONCURRENT_IMAGE_PROCESSING=4` semaphore + 3Gi backend limit. Audio-separator needs 6Gi for BS-Roformer (OOMKilled at 4Gi).
- Backend log `Unhandled exception` + `HttpMessageNotWritableException` is the unambiguous "5xx with body the client can't decode" signal. Grep before declaring a release healthy: `kubectl logs -n yay-tsa deployment/<backend> --since=5m | grep -E "Unhandled|MessageNotWritable|MessageConversion"`. (Async write-loop `AsyncRequestNotUsableException` on Pause/Skip is benign — swallowed by a no-body `@ExceptionHandler`.)

### Multi-device / SSE sync

- `useDeviceEvents` SSE hook must fetch the device list on mount — otherwise `updateDeviceState` maps over an empty array and drops every event. On an event for an unknown `deviceId`, refetch the full list.
- `broadcastDeviceState` must include `nowPlayingItemName` alongside `nowPlayingItemId` — otherwise DevicesPanel shows position but no track name.
- Group Sync SSE membership broadcast (`GroupEventBroadcaster`) is in-memory → single-replica only. Crawler timeout on SSE-heavy pages (`networkidle` never fires) is expected — those routes are in `schemathesis-exclude-paths`.

### Mobile E2E locator quirks

- Mobile viewport tests use a separate Playwright project (`[mobile]`, Pixel 7 412px).
- PlayerBar renders a mini-bar on mobile (`md:hidden`) and hides it via CSS `invisible` when the full player opens — `expect(playerBar.playerBar).toBeVisible()` FAILS while the full player is open; assert `playerBar.currentTrackTitle` instead. Test IDs must exist in BOTH the mini-bar AND MobileFullPlayer for `:visible` selectors. `ensureControlsVisible()` opens the full player and changes which elements are visible.
- `getByRole('button', { name: 'Shuffle' })` also matches the AlbumDetailPage text-button — use `locator('button[aria-label="Shuffle"][aria-pressed]:visible')` for player controls.
- Badge text may include a unit suffix ("15m") — parse with `Number.parseInt(text ?? '0', 10)`.
- `setPointerCapture` throws `DOMException` for synthetic pointer events — guard `handlePointerDown` with try/catch. Modal components must `createPortal` to escape invisible parents.
- Lighthouse LCP budget 3000ms (2500ms too tight for CI runners). Buttons with responsive text (`hidden sm:inline`) need explicit `aria-label` + `aria-hidden="true"` on the visible span.

### `core_v2_*` schema specifics

- **Cross-schema track-id refs are by-value, no FK**, in 11 tables (`favorites`, `play_history`, `play_statistics`, `queue_entries`, `playlist_tracks`, `track_features`, `user_track_affinity`, `karaoke.assets`, `adaptive.adaptive_queue`, `adaptive.listening_sessions`, `adaptive.playback_signals`). Any dedup/cleanup must remap all of them before dropping an `entities` row or favorites/history dangle.
- **Schema features ship dead.** ML embeddings, HNSW indexes, taste profiles, and `user_track_affinity` all existed in the DB and "looked deployed" while no production code read or wrote them for months. Treat every new schema feature as untrusted until a DB-backed integration test exercises a controller/worker that demonstrably reads/writes through it — the migration succeeding proves nothing will ever call it. Required end-to-end tests: embeddings drive bootstrap queue (≥50% overlap with `findSimilarTracks`), `PLAY_COMPLETE`/`SKIP_EARLY` signals move `affinity_score` within one worker tick, red-lines remove tagged tracks from recommendations, two distinct radio seeds yield non-identical sets, ArchUnit guards every injected `MlQueryPort` has a reachable `@Repository` impl. Operational signal: Micrometer `yaytsa.ml.affinity.last_updated_at` + Grafana alert on `>6h` stale.
- **Test-vs-prod column-type drift is a false-confidence trap.** Hibernate `ddl-auto` in Testcontainers creates `playback_signals.context` as **jsonb**; prod (Flyway/v1-ETL) is **text**. SQL using jsonb `->>` passes the integration test but floods prod with `BadSqlGrammarException` every ~60s. Fix that works on both: `substring(ps.context::text from '<regex>')`. A green integration test does NOT prove prod-safe SQL when schemas diverge — grep prod logs for `BadSqlGrammar` after any worker-SQL change.
- **`ddl-auto: validate` in prod** (own DDL via Flyway). `update` is a foot-gun: re-ALTERs columns on every boot, fails when rows exceed new bounds, races prepared-statement caches (`cached plan must not change result type`).
- **Placeholder vs missing tags.** Some FLAC rips store `##### ######` in TITLE/ARTIST/ALBUM (Cyrillic encode failure). `String.usableTag()` treats `^[#?\s]+$` as missing → folder/filename fallback. Filename-fallback titles strip leading `\d{1,3}\s*[-._]\s*`. Detection: `SELECT name FROM core_v2_library.entities WHERE entity_type IN ('ARTIST','ALBUM','TRACK') AND name ~ '^[#?\s]+$'`. If `ffprobe` shows the FLAC tag itself is the placeholder, code can only fall back — the cure is re-tagging the source. The mechanical `/qa` walkthrough checks (M1 mojibake, M2 tofu runs, M3 adjacent dups, M4 filename-in-title) catch this class — see `/qa` skill Phase 1.5.
- **Image rows rot** because the scanner walks audio files only and never inserts/repairs image rows — v1-ETL paths dangle whenever folders are renamed. Audiobook rips often carry art ONLY in tags: check `ffprobe -show_streams <file> | grep attached_pic` before treating an `/Images/Primary` 404 as a data bug. Fallback chain: image row → embedded art (item's own file, or first chapter for albums) → 404. An art-less album correctly 404s — the crawler flags it as a finding; gate-policy fix is upstream (autoqa), not app code.
- **TotalRecordCount on `/Items` must be a DB count, not `items.size`** — the PWA infinite-scroll stops at `loaded >= totalRecordCount`, so `items.size` caps a 9k-track library at "50 of 50". Sanity: `/api/Items?...&Limit=1 | jq .TotalRecordCount` must equal `SELECT count(*) ... entity_type='TRACK'`.

### springdoc / OpenAPI config

- **`OpenApiConfig.pathIdPatternCustomizer`** sets a `pattern` on every plain-string path param, declaring the real id alphabet so positive schemathesis fuzzing stops generating `;`-garbage that StrictHttpFirewall 400s. Biggest schemathesis FP-killer for this project.
- **`OpenApiConfig.globalResponsesCustomizer`** documents `application/problem+json` for 400/401/403/404/409/429/500 on every op. This is why every error body MUST be RFC7807 problem+json (filter-chain, controller, `HttpFailureTranslator`, and the custom `/error` `ErrorController` all converge on `dev.yaytsa.adaptershared.problemDetail(...)`). The PWA reads `errorData.message`, never `.error`.
- **`ResponseEntity<Map<String, Any>>`** makes springdoc emit `additionalProperties: {type: object}`; any array/scalar value in the real body violates it. Rule of thumb: any controller returning `Map<String, Any>` is a future schemathesis finding — return a typed DTO.
- **No-content endpoints (`204`) are a schemathesis "Undocumented HTTP status code" trap.** A handler returning `ResponseEntity<Void>`/`Unit` answers `204`, but springdoc only infers `200`, so a real `204` (e.g. `POST /v1/client-errors`) trips the gate. Declare `204` in `OpenApiConfig` via an `OperationCustomizer` that inspects `handlerMethod` return type (`noContentResponseCustomizer`) — NOT a swagger `@ApiResponse` annotation on the controller: the thin adapters (adapter-jellyfin) have no `springdoc`/`swagger-annotations` on their classpath, so the import is `Unresolved reference 'swagger'` in a clean `:adapter-jellyfin:compileKotlin` (it can pass on a warm local daemon — only the clean CI build is authoritative). All OpenAPI shaping lives in the `app` module's `OpenApiConfig`.
- **When the autoqa gate reports "schemathesis reported N failure(s)" with N small, find the odd-one-out.** The gate reconciles the chronic `text/html` 4xx (Tomcat/nginx pre-servlet rejections) to informational, so a residual `N=1` blocking is usually a REAL conformance bug hiding among dozens of reconciled control-char 400s — read every `FAILURES` block, not just the first. The `204`-undocumented client-errors case surfaced exactly this way.
- **Binary streaming bypasses Spring's message-converter pipeline** — `/Audio/{id}/stream` takes `HttpServletResponse` and writes bytes directly. Any Kotlin `ResponseEntity<*>` (erases to `Any`) or unrecognized codec string for binary payloads silently fails `canWrite` → `HttpMessageNotWritableException` 500. Codec match is substring-based (`"FLAC 16 bits"` contains `flac`) with file-extension sniffing and `audio/mpeg` last-resort.
- **Wrong `Content-Type` on a JSON `@RequestBody` endpoint must map to 415, not 500 — and schemathesis cannot catch this.** Schemathesis fuzzes bodies but sends the OpenAPI-declared `application/json`, so it never exercises a mismatched content-type; only a manual `curl -H 'Content-Type: text/plain' --data-binary $'\x00'` (or `application/x-www-form-urlencoded`, or no CT) does. Spring throws `HttpMediaTypeNotSupportedException`; if `GlobalExceptionHandler` lacks a handler for it the request falls through to `handleGeneric` → **500 + `log.error("Unhandled exception")` on every malformed client request** (5xx lying about a 4xx, plus error-log spam that masks real errors). Fix = explicit `@ExceptionHandler(HttpMediaTypeNotSupportedException)` → 415 (and `HttpMediaTypeNotAcceptableException` → 406), with 415/406 added to `OpenApiConfig.STANDARD_ERROR_RESPONSES`. Detect every pass: `kubectl logs deploy/yay-tsa-v2-production-backend --since=1h | grep -c "HttpMediaTypeNotSupported"` should be 0; if a `/qa` content-type fuzz produces a 500, it's this gap. (Found + fixed 2026-06-21; issue #252's CF-520 was a separate rollout-transient, but the investigation surfaced this real 500-vs-415 gap.)

For the generic forms of these — Spring `ResponseEntity<String>` charset, `Map<String,Any>` schemathesis, StrictHttpFirewall 400, springdoc `http://` in `servers`, the filter-chain RFC7807 auth-entry-point, reverse-proxy base-url, binary/Range/206, and schema-shipped-but-unwired — see `/qa` skill `API conformance QA`, `Auth / session QA gotchas`, and the `Browser QA` binary bullet. Stale review artifacts (a `REVIEW_SECURITY.md` listing IDOR/MD5/ACTUATOR findings already remediated the same day): verify every stale review artifact against current HEAD before acting on it.

### SonarCloud (project-specific)

- Automatic Analysis (no `sonar-scanner` in CI) — `sonar-project.properties` is ignored; exclusions set via web UI/API.
- **`yay-tsa-v2/` is INCLUDED in the scan since 2026-07-13** (v2 IS the production backend; the old "must stay excluded" rule is retired). Two one-time artifacts of lifting the exclusion, both triaged 2026-07-14: (1) **stale issues resurfaced with their ORIGINAL creation dates** (2026-04-18), including 6 phantom `githubactions:S7637` findings on `yay-tsa-v2/.github/workflows/ci.yml` — a file that does not exist at HEAD (subdirectory workflows never ran anyway); before treating a "new" v2 issue as real, check the file exists and the `creationDate`. Phantoms → `wontfix` with comment. (2) ~296 v2 code smells count as "new code" (baseline flood); detekt already gates v2 with per-module baselines, quality gate stays green — don't drive-by-fix the flood. Test-fixture S2068/S6418 "hard-coded credential" hits in `app/src/test` + `core-testkit` + v2 `docker-compose.yml` dev defaults are FP (marked with comments).
- **S3735 ("remove `void` operator") is a standing conflict** with the enforced `@typescript-eslint/no-floating-promises` (`eslint.config.js`): the codebase uses `void promise()` deliberately for fire-and-forget, and removing it fails ESLint + reintroduces floating-promise bugs. Don't "fix" these — they're bulk-accepted in Sonar. New `void` calls in future code will re-raise S3735; accept, don't rewrite.
- **S7766 false positive** on `useAudiobooks` `reduce` over `resume.updatedAt`: those are ISO-timestamp STRINGS, so `Math.max` would coerce to `NaN`; the string-comparison ternary is correct (marked FP).

### Test-artifact hygiene

- Schemathesis/Hypothesis writes `.hypothesis/` in CWD — gitignore at workspace root AND in every dir pytest runs (`packages/core/`, `services/server/`).
- One-shot QA artifacts (`openapi.json`, `ANALYSIS.md`, `login-*.png`, `qa-*.{png,jpeg}`) accumulate at repo root — keep them gitignored.
- `kubectl cp` to CNPG pods fails (read-only fs) — pipe via stdin: `kubectl exec -i ... -- psql ... < script.sql`. Mutations must target the primary pod (`kubectl get cluster -n shared-database -o jsonpath='{.items[0].status.targetPrimary}'`); replicas reject as `read-only transaction`.

---

Generic patterns (CI continue-on-error/aggregate-gate, two-CI-cancel, ArgoCD Image-Updater write-back & disabled-service, kubectl connection-reset, Helm chart versioning, ZAP SQLi rate-limit FP, Sonar S3735/S7766, accessibility, pgbouncer idle-connection) live in the global `/qa` skill — do not duplicate here.

## post-deploy-qa must wait for the LAST BACKEND-BUILT sha, not the current sha

`post-deploy-qa` (in `ci.yml`, runs on **every** push to main) shares the `wait-backend-rollout` composite with `v2-ci.yml`. The action polls `/api/System/Info/Public` `BuildSha` for `main-<sha7>`. In `v2-ci` that's correct (it only runs on backend-path changes, so the current sha IS the new backend tag). In `ci.yml` it is NOT: the backend image is rebuilt only when `v2-ci.yml`'s path filter matches (`yay-tsa-v2/**`, `charts/yay-tsa-v2/**`, the v2-ci workflow), so a **frontend- or docs-only push never produces a `main-<this-sha>` backend image** and the wait times out forever — every non-backend commit's post-deploy-qa went red on "Backend never settled". Fix (shipped): `ci.yml` resolves the sha of the last backend-touching commit (`git log -1 -- yay-tsa-v2 charts/yay-tsa-v2 .github/workflows/v2-ci.yml`, needs `fetch-depth: 0`) and passes it to the action via the new `commit-sha` input. When triaging a red post-deploy-qa, first confirm it's not this class (only relevant if the wait step itself fails).

## Helm chart releases silently dropped — Forgejo push-mirror clobbers GitHub gh-pages

The chart repo is GitHub Pages `gh-pages`, authored ONLY by GitHub Actions (`release-chart.yml`). But the Forgejo→GitHub **push-mirror** mirrors all refs, including `gh-pages`, and the reverse-sync cron (`gitops:.../cronworkflow-github-reverse-sync.yaml`) only synced `main` + bot branches back GitHub→Forgejo — `gh-pages` fell through untouched. Net: every chart release pushed to GitHub's gh-pages was reverted to Forgejo's stale copy on the next mirror cycle, freezing the published chart (and silently dropping all chart changes). ArgoCD shows `Synced`+`Healthy` because it faithfully deploys the newest-in-range published chart — which is just stale. **Detection:** compare `curl -s https://nikolay-e.github.io/yay-tsa/index.yaml` newest `yay-tsa-v2` version + `created` date against recent chart commits; or `gh api 'repos/nikolay-e/yay-tsa/commits?sha=gh-pages'` HEAD date. If the live deployment lacks a chart change that's merged to main (e.g. a configmap/deployment env that the chart template clearly renders), suspect this, not the chart template. Fix (shipped, gitops): add `gh-pages` to the reverse-sync force-sync case so GitHub stays authoritative for it.

## A "live config lacks a chart key" symptom usually means stale chart, not a chart bug

When a `kubectl get configmap/deployment` shows the live render missing a key the **current** chart template renders (e.g. backend env `LLM_API_KEY`, configmap `LLM_BASE_URL`), the chart is almost always fine — the cluster is running an older published chart. Confirm by diffing the live configmap/deployment env against `helm template charts/<chart>` locally; if they differ, it's a publish/sync gap (see the gh-pages section above), not a template fix. The recurring `LlmClient: LLM enabled but no API key configured` WARN every 30s is the canonical example: secret `LLM_API_KEY` exists, chart template injects it under `llm.enabled`, but the deployed (old) chart had no such env block.

## `:app:test` flake lottery has TWO distinct shared-context root causes (a different test fails each run)

The `:app:test` suite runs every integration class in ONE Spring context against ONE Postgres, committing seed rows via `JdbcTemplate` (no per-test rollback). Two independent races make it fail non-deterministically — a _different_ class fails each run, which masks that these are systemic, not per-test, bugs. Both fixed 2026-06-21; if `:app:test` goes red intermittently, check it's not a regression of either before chasing the named test:

1. **Auth rate-limit bucket sharing.** `/Users/AuthenticateByName` is rate-limited per client IP (`remoteAddr` + URI). All MockMvc requests default to one `remoteAddr`, so logins across every class share ONE bucket and 429 intermittently — surfacing as a login `assertEquals(200)` failing deep inside a helper (e.g. `SubsonicApiIntegrationTest.issueApiToken` → cascades to whatever test called it). Fix: a shared `uniqueClientIp()` `RequestPostProcessor` in `HttpIntegrationTestBase` stamps a distinct synthetic IP per login; every token-issuing login (`AuthIntegrationTest`, `SubsonicApiIntegrationTest`) must `.with(uniqueClientIp())`. A GET-with-empty-creds 401 check is not a token-issuing login and doesn't need it.
2. **Scheduled library scan reconciling against the empty test music-path.** `LibraryScanner.scan()` is `@Scheduled(initialDelay=5_000)` and fires on a background thread ~5s into the JVM, walking `yaytsa.library.music-path` (set to the empty `tmpdir` in the base) and deleting orphan artist/album `entities` — including rows a test seeded microseconds earlier (track inserted last, so artist/album look orphaned), throwing `albums_artist_id_fkey` FK violations mid-`@BeforeEach`. `ItemsEndpointBenchmark` already carried a band-aid guard against exactly this. Fix: gate the scheduled run behind `yaytsa.scanner.scheduled-enabled` (default `true` for prod, set `false` in `HttpIntegrationTestBase`); the manual `LibraryScanTriggerPort` and direct `LibraryScanner(writer, root).scan()` constructions (scanner-module hygiene tests) keep working via the Kotlin `= true` default. Don't "fix" by pointing music-path at a missing dir (changes streaming/scan-trigger tests) or disabling all scheduling.

## Testcontainers reused-PG: serialize CREATE EXTENSION (pg_extension_name_index race)

`SharedPostgresContainer` uses `withReuse(true)`, so the parallel per-module Gradle test JVMs share ONE physical Postgres. `CREATE EXTENSION IF NOT EXISTS` is NOT concurrency-safe — two JVMs can both pass the existence check and both INSERT, one failing with `duplicate key value violates unique constraint "pg_extension_name_index"`. It surfaces as a context-load `IllegalStateException` cascading to every test in one class (often `AdaptiveQueryPortTest`), intermittently, on commits that don't even touch Kotlin. Fix (shipped): wrap the extension creation in `SharedPostgresContainer` in a session advisory lock (`pg_advisory_lock`) and pre-create every extension any module's Flyway needs (citext, vector, pg_trgm, unaccent) so downstream `IF NOT EXISTS` calls are guaranteed no-ops. A single green re-run does NOT disprove this class (it's a race); reason about the lock, don't trust one pass. Don't "fix" it by disabling parallelism or reuse.

## Backend-rollout 502s in post-deploy-qa (when a /qa pass ships backend changes)

`post-deploy-qa` now waits for the backend (last-built sha, see above) before fuzzing, so mid-rollout 502 bursts should be rare. If they still appear: When the same pass also changes the Kotlin backend (new image → legitimate rollout), the schemathesis phase can land mid-rollout and report a burst of **`502 Bad Gateway` (Cloudflare HTML)** as "Server error: N" — plus derived "JSON deserialization error" / "Undocumented Content-Type/HTTP status" from the 502 HTML body. These are transient deploy artifacts, NOT app bugs; verify by reading the 502 body (`yay-tsa.com | 502: Bad gateway`) and by confirming crawler/axe (frontend) stayed at 0 violations and ZAP HIGH = 0. The authoritative clean schemathesis run is the next post-deploy-qa that lands entirely **after** `kubectl rollout status deploy/yay-tsa-v2-production-backend` reports complete and the new pod is `1/1 Running` with a settled age. Don't file the 502s.

## Metadata enricher (MusicBrainz + Cover Art Archive)

`infra-metadata-enricher` is a `@Scheduled`+`@ConditionalOnProperty(yaytsa.metadata.enabled)` worker bean in the single backend bootJar. It writes `artists/albums.musicbrainz_id` (+ `release_group_mbid`) and drops `cover.jpg` into each album's own folder. Verify it in prod: `kubectl logs deploy/yay-tsa-v2-production-backend -n yay-tsa --since=10m | grep MetadataEnricher` shows `cycle starting`/`cycle complete: N artists, N albums`; DB progress `SELECT count(*) FROM core_v2_library.albums WHERE release_group_mbid IS NOT NULL` on the CNPG primary.

- **Writes need the media mount writable.** The cover lands under `music-path` (so the existing `/Items/{id}/Images` serves it with no code change), but the `media` volume is `readOnly: true` by default in the chart. Prod sets `backend.media.readOnly: false`. With a read-only mount the enricher silently can't write art (MBIDs still set).
- **External-provider clients MUST throw on transient failure, never swallow as empty.** Idempotency uses `metadata_checked_at` (set after every attempt so no-match never re-queries). If the MusicBrainz/CAA client returns an empty list on a timeout/5xx/429 (indistinguishable from a genuine 404/no-match), the enricher marks the row checked and the album is **permanently** skipped — a whole cluster-outage cycle silently burns every row it touched. Correct contract: client throws `MetadataProviderUnavailableException` on network error / exhausted-retry / 429/503/5xx, returns null only on 404. The per-item `try/catch` then leaves `metadata_checked_at` unset → retried next cycle. Recovery for already-burned rows: `UPDATE core_v2_library.albums SET metadata_checked_at = NULL WHERE metadata_checked_at IS NOT NULL AND musicbrainz_id IS NULL AND release_group_mbid IS NULL;` (and artists on `musicbrainz_id IS NULL`) on the primary — re-queues only un-enriched rows.
- **Egress check from the pod**: `kubectl exec <pod> -- wget -T12 -qO- 'https://musicbrainz.org/ws/2/artist?query=artist:Radiohead&fmt=json&limit=1'` returns **403** (busybox sends no User-Agent; MB blocks blank UAs) — that 403 still proves DNS + connectivity work. The real client sends the mandatory UA. CAA returns 307 (redirect to archive.org). A `connect timed out` in enricher logs right after a cluster outage is residual node-DNS flakiness (CoreDNS restarting), not an egress block.

## Local composite action needs `actions/checkout` in its job

Both post-deploy jobs (`post-deploy-qa` in `ci.yml`, `post-deploy-verify` in `v2-ci.yml`) call `uses: ./.github/actions/wait-backend-rollout`. A **local** action only exists on the runner after `actions/checkout` runs in that same job — without it the step fails setup with `Can't find 'action.yml'… Did you forget to run actions/checkout before running your local action?`, which then cascades (e.g. `ZAP report not generated` because AutoQA never ran). When extracting an inline `run:` block into a shared `.github/actions/*` composite, add a `Checkout repository` step to **every** job that now `uses:` it — the post-deploy jobs historically had no checkout because they only ran inline scripts and remote actions.

## LLM-DJ routes through the in-cluster LiteLLM gateway (GPT-5.4 Mini)

`infra-llm` `LlmClient` calls the OpenAI-compatible `/v1/chat/completions` on `http://litellm.litellm.svc.cluster.local:8080` (Bearer `LLM_API_KEY`, model `GPT-5.4 Mini`), not the Anthropic API. Verify go-live: `kubectl logs deploy/yay-tsa-v2-production-backend -n yay-tsa --since=5m | grep LlmOrchestrator` shows `LLM-DJ added N tracks to session …` (the end-to-end proof the gateway call + OpenAI-format parse + queue-edit apply all work). Three wiring dependencies, all in gitops: (1) the backend's namespace (`yay-tsa`) must be in litellm's `gatewayClientNamespaces` (else its NetworkPolicy blocks ingress — symptom: `Connection refused` to `litellm:8080`); (2) `LLM_API_KEY` (a dedicated LiteLLM virtual key, SOPS-encrypted) in `yay-tsa-production-secrets`; (3) `values.prod` `backend.config.llm.enabled: true`. The key has a hard $/30d budget so an exhausted key 429s → `LlmClient` falls back to ML-only, never crashes.

- **ksops secret edits can show a false ArgoCD `Synced`.** After `sops set` adds a key, the `secrets-yay-tsa` app may report `Synced` while the live Secret still lacks it (its last real sync op was weeks old; the refresh comparison didn't re-apply). Force it: `kubectl patch application secrets-yay-tsa -n argocd --type merge -p '{"operation":{"sync":{"revision":"HEAD","syncStrategy":{"apply":{"force":true}}}}}'`. If a deployment references a secret key mid-rollout before the secret syncs, the pod fails container-create with `couldn't find key LLM_API_KEY in Secret …` and recovers once the key lands.
- **`encrypt-data` hotspot on the in-cluster `http://litellm…` URL** (chart configmap/values) is a SonarCloud FP → mark `REVIEWED/SAFE`: cluster-internal ClusterIP traffic confined by NetworkPolicy, TLS not applicable.

## Audiobook card: covers, chapter-advance, offline, mark-finished (2026-07-05)

Four audiobook defects fixed this pass — all verified live (frontend `main-a6f9507`, backend `main-1dc0954`):

- **Covers** come by **AlbumId**, never the chapter track id. Chapter tracks carry no own image row and the backend track DTO emits **no `AlbumPrimaryImageTag`** (confirmed: `/v1/me/audiobooks` sample has `AlbumPrimaryImageTag=None, ImageTags=None`). `useAudiobooks.buildBook` sets `coverItemId = first.AlbumId` and a synthetic tag fallback (`… ?? coverItemId`) so `MediaCard` actually fires `/Items/{albumId}/Images/Primary`. Of the 4 prod audiobook albums, 3 (Спин/Вихрь/Ось) have covers (200 image/jpeg, `naturalWidth 320`); **«Птица малая» (e5ff195a) is art-less → 404 → placeholder**. That 404 is an **expected decorative console error** (telemetry drops IMG resource errors at source; crawler ignores it) — the client cannot know art-lessness ahead of time, so it can only be removed by the backend emitting a real album image tag on track DTOs (a backend round-trip not worth it for one decorative 404). Do NOT treat the «Птица малая» cover 404 as a regression.
- **Mark-finished** fans out over **every** chapter incl. never-started ones; backend `ResumePositionService.markFinished` must **upsert** a finished row (previously update-only → `null` → HTTP 404 on unstarted chapters → `Promise.all` rejects → tab never invalidates → button looked dead). Frontend uses `Promise.allSettled` + `onSettled: invalidate` so one 404 can't abandon the rest.
- **Chapter-skip (every-other)** was a background-wake race: `recoverStalledAdvance` (fires on `visibilitychange`/`focus`/`pageshow`) preempted an in-flight cooperative `onEnded` advance and re-read the already-moved queue index → +2. Fix = bail `if (controller.isActive) return` at the top of `recoverStalledAdvance`. Not reproducible headless (needs a locked-screen background wake); verify chapter _display_ order instead (IndexNumber sort, 01→02→03… no gaps).
- **Offline for books**: `BookCard` had no download control and audiobooks are stripped from every library surface by `excludeGenres`, so the only route into IndexedDB was the evictable listening-cache. Added `DownloadTracksButton tracks={book.chapters.map(c=>c.item)} reason="manual"` (one "Download" per book, `reason=manual` shields from LRU). Verify: 4 cards → 4 `download-album-button`.

**Browser-QA gotcha this surfaced:** the audiobook covers + download button read as _absent_ (all placeholders, 0 download buttons) purely because the test browser served a **stale SW-cached bundle** (`index-D4gvXXCN.js`) while prod served `index-CTfuAYCb.js`. ALWAYS `serviceWorker.getRegistrations().unregister()` + `caches.keys().delete()` + reload before concluding a frontend change didn't deploy — a "missing" feature is usually stale cache, confirm the loaded `/assets/index-*.js` matches the prod bundle first.

## NUL byte in a search term → PostgreSQL 22021 + ERROR-log spam (2026-07-05)

A search term containing a NUL byte (`%00` in the query-string VALUE — passes StrictHttpFirewall, which only blocks NUL in the PATH) reaches `pg_trgm`/`ILIKE` as a bind parameter, and PostgreSQL rejects `0x00` with **`SQLState 22021` "invalid byte sequence for encoding UTF8: 0x00"**. The client still gets a correct **400** (the exception is caught), but Hibernate's `SqlExceptionHelper` logs **WARN + ERROR** for every such request — under a monkey/crawler fuzz burst that floods the backend log with ERROR lines that mask real errors. Detect: `kubectl logs deploy/yay-tsa-v2-production-backend --since=30m | grep -c "22021"` should be 0. Reproduce: the fuzz string was `SearchTerm=🐒💥🔥𝕏‮%00` (emoji + U+202E RTL-override + NUL). Fix (shipped): `LibraryQueries.scrubQuery` strips NUL (`query.filter { it.code != 0 }`) at the single application-layer entry, applied in `searchText` + `countTextSearch{Tracks,Artists,Albums}` — covers every protocol adapter (Jellyfin/Subsonic/MCP/Adaptive). Same class as the documented `HttpMediaTypeNotSupported → 500` input-validation gap: untrusted free-text must be sanitized before it hits the DB, not left to throw. Integration test in `ItemsPaginationIntegrationTest` (NUL search → 200, still matches).

**Editing gotcha (cost me a corrupted-source round):** writing a Kotlin `"\u0000"` string escape via the Edit tool wrote a **literal 0x00 byte** into the `.kt` source (the tool interpreted the escape). Kotlin compiled it, but a raw NUL in source is unacceptable (breaks spotless/git/readers). Use a code expression that needs NO string escape: `query.filter { it.code != 0 }` (strip) or `0.toChar()` (construct a NUL in a test) instead of `"\u0000"`. Verify with `perl -ne 'print if /\x00/' <file>` — `grep -c $'\x00'` is useless in zsh (empty pattern matches every line).

## Audiobook exclusion is layered (genre `Audiobook`)

Three independent filters, by surface: (1) main library `/Items` — backend `ExcludeGenres` param (count-correct, drops `TotalRecordCount`; the PWA sends it on `useInfiniteTracks`); (2) daily-mix + radio-seeds — backend adaptive filter (`isAudiobookTrack` in `JellyfinAdaptiveController`), because the `RadioSeed` DTO carries no genre so the client cannot filter it; (3) daily-mix also has a client `!isAudiobook` belt-and-suspenders in `DailyMix.tsx`. If audiobooks reappear in radio/daily-mix it's the **backend** (check it's deployed); if in the main library list with a wrong count, it's the `ExcludeGenres` query. Favourites filter uses `IsFavorite=true` (the Jellyfin `Filters=IsFavorite` form is ignored and returns the full library — don't use it to count favourites).

## LLM-DJ over-spend check (the `@Scheduled` orchestrator is a paid side-effect)

`LlmOrchestrator.processActiveSessions` runs every 30s over every un-ended adaptive session and calls the (billed) LiteLLM completion. **A correct run is near-silent.** Smoke it by the call rate, not by "it works": `kubectl logs deploy/yay-tsa-v2-production-backend -n yay-tsa --since=2m | grep -c 'LLM-DJ added'` should be roughly the number of sessions that saw _new_ listening signals in that window — single digits, not ~30. A steady ~15/min means the freshness gate regressed (the orchestrator must skip a session when its newest signal id equals the last decision's `triggerSignalId`; without that gate every active session is re-billed every tick forever). The dedicated LiteLLM key has a hard $/30d budget so the worst case degrades to ML-only (graceful), but a high steady rate is a real cost bug to fix at the gate, never by disabling the DJ. Token-per-decision audit columns (`llm_decision.prompt_tokens/completion_tokens`) are currently unpopulated — don't rely on them for cost; count log lines or read the LiteLLM key's spend.

## Run the FULL module test suite before pushing a domain-logic fix

A domain fix can be correct yet break an **existing** test that encoded the old (buggy) behavior — and the `core-domain:playback` suite runs in `./gradlew build`, so CI catches it, but only after a ~15min build + the Testcontainers flake lottery. Example: the `play()`-resume position fix (snapshot `computePosition` instead of keeping the stale `lastKnownPosition`) was correct, but `PlaybackHandlerTest "Play with current entryId preserves position"` asserted the rewound `42s` (the bug) instead of the true `47s`. Compiling the changed module + running only the _new_ test is NOT enough — run the changed module's whole `:test` task (`./gradlew :core-domain:playback:test`) locally before pushing. When an existing test fails on a domain fix, first decide whether it encodes the bug (update the assertion to the correct value, with a comment) vs. a real regression (revert) — never blindly flip it green.

## Client-error telemetry review (every /qa pass)

The PWA ships sanitized client-side error reports to `POST /v1/client-errors` (unauthenticated, bucket4j 30/min/IP). Backend writes one compact JSON line via the dedicated `client-error` SLF4J logger (→ stdout → Promtail → Loki) and increments `yaytsa_client_errors_total{category,type,version}` (+ `yaytsa_client_errors_dropped_total{reason}`). This is the ONLY window into client-only failures that never reach the backend (render crashes, offline DECODE errors, CSP violations). **Reviewing it is the point of the feature — read the captured errors and fix the real bugs, don't just confirm the pipe is alive.**

Coordinates: namespace `yay-tsa`, deployment `yay-tsa-v2-production-backend`, logger name `client-error`, public URL `https://yay-tsa.com/api/v1/client-errors` (nginx/Traefik strip `/api` → backend `/v1/client-errors`).

**`kubectl logs` is NOT the source of truth — it truncates at the current pod's start.** A backend rollout (every `/qa` that ships a backend change, or any Image-Updater bump) restarts the pod, so `kubectl logs --since=24h` only shows minutes of history and will read as "almost no client errors". The authoritative all-time view survives restarts in two places: **Loki** (`{namespace="yay-tsa"} |= "client_error"` over 7–30d — full message/`fp`/`category`/`type`/`version` content) and the **Prometheus counter** via `sum by (category,type,version) (increase(yaytsa_client_errors_total[30d]))` (the in-pod `yaytsa_client_errors_total` instant value resets on restart; `increase()` reconciles resets). Port-forward `svc/prometheus-prometheus:9090` and `svc/loki:3100` in `monitoring` (read-only, fine for `/qa`). Triage every distinct `fp` against the deployed **frontend** `version` tag (the tag IS the frontend app version): a class that appears only on versions OLDER than a fix is already-fixed — confirm by checking the current frontend version's counter is 0 (browse a page that would trigger it, then re-read). The recurring `resource/ResourceError` `IMG …/Images/Primary` class is decorative cover-art 404s, suppressed at source by the "drop decorative img telemetry" change — expected to be 0 on current frontend even with coverless items on screen.

### Pull the captured errors

```bash
# Structured client-error lines (last 24h), pretty
kubectl logs -n yay-tsa deploy/yay-tsa-v2-production-backend --since=24h 2>/dev/null \
  | grep '"src":"client_error"' | sed 's/.*: {/{/' | python3 -m json.tool 2>/dev/null | less
# Group by fingerprint (which distinct bugs, how often)
kubectl logs -n yay-tsa deploy/yay-tsa-v2-production-backend --since=24h 2>/dev/null \
  | grep -o '"fp":"[^"]*"' | sort | uniq -c | sort -rn
# Counter breakdown (Prometheus / Grafana): yaytsa_client_errors_total by category,type,version
#   and yaytsa_client_errors_dropped_total{reason=oversize|malformed}
```

### Triage rule (zero-deferral, like every /qa finding)

- **Every distinct fingerprint is a real client-only defect** → reproduce + fix or `gh issue create`. The whole reason this channel exists is that backend `http.server.requests` 5xx can't see these.
- **Post-deploy spike** on any fingerprint right after a release = regression signal — bisect against the `version` tag.
- **Tag-domain health**: `category`/`type`/`version` are server-coerced to finite domains. A surge of `type="other"` = a real JS error class worth adding to the controller allow-list; a surge of distinct legit-looking but non-`main-<sha>` `version=unknown` or a flood of `dropped{reason=malformed}` = someone probing the public endpoint (cardinality/log-flood attempt), not an app bug — confirm bucket4j is holding.
- **Redaction audit (🔴 if it fails)**: spot-check `msg`/`stack` for any surviving `api_key`/`Bearer`/`token=`/`X-Emby-Token`/high-entropy run. A live leak means the redaction missed a vector → fix the server regex immediately (client redaction is bypassable).

### Live pipeline smoke (validates strip-prefix + permitAll + CSRF-off + redaction in prod — closes must-fix #6)

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST https://yay-tsa.com/api/v1/client-errors \
  -H "Content-Type: text/plain" \
  --data-binary '{"category":"runtime","type":"TypeError","message":"QA-SMOKE api_key=LEAKTEST","appVersion":"qa-smoke"}'   # expect 204
kubectl logs -n yay-tsa deploy/yay-tsa-v2-production-backend --since=2m | grep QA-SMOKE   # expect api_key=[REDACTED], single line
```

Synthetic smoke rows carry `version=qa-smoke` → coerced to `unknown` (not a real `main-<sha>` tag), so they never pollute the per-release metric; filter them out of log review by `version!=qa-smoke` in the message.

### Known class: backend-rollout transient burst (best-effort, fixed 2026-06-24)

**Reading method (don't repeat my mistake):** the `yaytsa_client_errors_total` Prometheus counter is PER-POD and resets on every backend restart, so a 48h `increase()` query after a recent rollout only shows the malformed-probe noise from the current pod's lifetime and reads as "almost no errors" — it MISSED a whole real-error cluster. **Loki is the authoritative 48h+ source** (QA.md says so; I ignored it first). loki-0 has no `wget`/`curl` and `loki:3100` refuses cross-pod from prometheus — so `kubectl -n monitoring port-forward svc/loki 13100:3100` and curl `/loki/api/v1/query_range?query={namespace="yay-tsa"}|="client_error"` from the host. Filter real errors with `select(.version|test("^main-"))` — `version=unknown` is smoke (`qa-smoke`) or public-endpoint probes (also the `type=malformed` / `dropped{reason=malformed}` rows), NOT app bugs.

The real cluster (one user, version `main-26b4985`, one ~30s window) was ~10 entries that were all ONE root cause — the backend briefly unreachable during a rollout: 5× `GET …recommend/* | /Items | /me/devices network error, retrying`, `POST …/signals failed`, `DELETE /v1/sessions/… failed`, `POST …/heartbeat Failed to fetch`, `Failed to send playback signal`, `Failed to end DJ session gracefully`. The PWA forwarded every one to error/warn telemetry. Fixes:

- **Per-retry "retrying" logs → `debug`** (`api.client.ts`): a retry in progress is not a failure; only EXHAUSTED retries log `error` (real). Kills the 5 `API:warn …retrying` whenever the retry then succeeds, without hiding a true all-retries-failed.
- **Fire-and-forget calls → `bestEffort`** (heartbeat, DJ `sendSignal`, DJ `endSession`): `post/delete(..., bestEffort=true)` downgrades THAT call's network/HTTP-error log to `debug` (sink doesn't forward debug). The caller already ignores these.
- **Sustained-failure escalators preserved**: `useDeviceHeartbeat` warns after 3 consecutive (~45s); DJ death still surfaces via `refreshQueue`'s `MAX_REFRESH_ERRORS` → `error`. So transient = silent, sustained = surfaced.

**Pattern: a retry-in-progress is `debug`, not `warn`; a fire-and-forget background call uses `bestEffort` + a consecutive-failure escalator. Never blanket-`debug` (hides real outages), never blanket-`error`/`warn` (every backend rollout then spams telemetry).**

## Affinity worker: signal-time cursor migration double-counts a pre-populated projection

`core_v2_ml.user_track_affinity` is a **regenerable additive projection** of `core_v2_adaptive.playback_signals`. When the aggregator's incremental watermark was changed from `MAX(updated_at)` (aggregation wall-clock) to a signal-time cursor (`core_v2_ml.affinity_cursor.last_signal_at`, V004), the cursor was seeded to `'epoch'` — correct only for an EMPTY table, but the prior wall-clock aggregator had already populated it. The first signal-time run re-folded ALL history additively onto existing rows → cumulative counters inflated (observed `sum(play_count)=3061` vs `PLAY_START` signal count `1194`, ~2.5× — old overlap + the re-fold compounded). Fix: truncate the projection + replay the cursor from epoch so every signal folds exactly once (live one-shot + durable migration V005). **Verification invariant (use every pass after touching affinity SQL):** `SUM(play_count)` MUST equal `count(*) FROM playback_signals WHERE signal_type='PLAY_START'`, and `SUM(skip_count)` MUST equal the SKIP\_\* signal count — these reconcile the projection against the source-of-truth log. After the fix both matched exactly (1194 / 314). The additive-fold has a known residual late-arrival edge (a signal committing with an EARLIER created_at than an already-consumed one is skipped) — documented in CORRECTNESS.md, needs a per-signal-id ledger or recompute-from-history, not a lookback (which would re-double-count). General rule: a watermark/cursor migration onto a pre-populated additive projection is a double-count trap — seed the cursor to the prior watermark OR truncate+replay; never seed `epoch` against a non-empty additive table.

## MetadataEnricher: a provider-unavailable exception must park too, or it starves the OFFSET-0 tail

The enricher's `selectFreshCandidates` fetches `OFFSET 0` ordered candidates and advances the effective scan window past permanent failures ONLY by parking them in the in-memory cooldown map (`coverAttemptCooldownUntil`). A cover lookup that returns a clean negative (404) is parked and drops out next cycle — but a lookup that **throws** `MetadataProviderUnavailableException` (CAA/archive.org connect/read timeout, 429/503/5xx — note CAA 307-redirects to archive.org, which is the part that times out) was historically only logged, never parked. Such an entity then sits at the head of the `OFFSET 0` query every poll cycle forever: it re-hammers the provider (~once per coverless sibling track, e.g. all chapter-tracks of one audiobook share one release-group) and **starves every coverless entity behind it** — the exact tail-starvation `selectFreshCandidates` exists to prevent. Symptom in Loki: dozens of `Audiobook cover deferred (provider unavailable)` WARNs per hour all citing the **same** `release-group`, while the coverless-count barely moves. Fix: park on the exception path too, with a SHORTER `cover-provider-retry-cooldown-hours` (default 6h) than the clean-negative `cover-retry-cooldown-hours` (168h) — transient outages still self-heal after the short window, permanent timeouts stop hammering and let the window advance. The album/artist paths don't show this in prod because a thrown exception leaves `metadata_checked_at` unset and they retry via the `metadata_checked_at IS NULL` union regardless — but the audiobook path is purely image-driven, so parking is the only throttle. Verify the candidate pool isn't a mis-resolution: `SELECT count(DISTINCT release_group_mbid)` over the coverless set — many distinct albums sharing ONE release-group would be a `ReleaseMatcher` bug, but many chapter-tracks of one album sharing it is normal.

## Live music-surface audiobook exclusion check (black-box)

DB has 175 audiobook-genre tracks in a 7354-track library (~2.4%). The personalized `/Items?IncludeItemTypes=Audio&SortBy=DatePlayed&StartIndex=0` feed includes a `browseTracksRandom` fill, so an UNFILTERED feed leaks ~2 audiobooks per 95-item page statistically. Verify exclusion live: pull the feed with `Fields=Genres` 3× (random fill resamples) and assert 0 items have an `audiobook` genre. After the `MusicSurfaceFilter` wiring on `buildPersonalizedTracks`, all 3 samples were 0/95. `Genres=Audiobook` as an `/Items` query param is NOT the audiobook mechanism (returns the full set) — the marker is per-track `genre`, joined via `entity_genres`/`genres`.

## Client-error telemetry QA (methodology)

- **Live endpoint smoke** (no kubectl needed): `curl -X POST https://<host>/api/client-logs -H "Origin: https://<host>" --data '<json>'` must return **204**; a malformed body and a >16KB body must also return 204 (never 5xx). yay-tsa's path is `/api/v1/client-errors`.
- **no-Origin reachability** (unload-beacon sim): `curl -X POST <endpoint> --data '<json>'` WITHOUT an Origin header must return 204 (the beacon fires on pagehide with no Origin). A 403 means the endpoint isn't exempt from the origin/CSRF gate — a real bug (caught lingua-quiz this way).
- **Browser transport check**: in Playwright MCP, `navigator.sendBeacon('/api/v1/client-errors', body)` returns `true` and a `fetch(..., {keepalive:true})` to the same path returns 204 — proves the real-browser same-origin transport.
- **console-guard fixture** (`tests/e2e/fixtures/console-guard.fixture.ts`): every mocked spec must import `test`/`expect` from it (auto-fixture asserts 0 console.error + 0 pageerror + 0 unexpected 4xx/5xx). Specs with bespoke `/api` routing must also call `stubSse(page)` from `helpers/media-fixtures.ts` — the app opens an EventSource on `/v1/me/devices|groups/:id/events`; a JSON catch-all makes Chromium log `EventSource's response has a MIME type` (mocked-env artifact). Per-spec allowlists are **string** patterns via `test.use({ allowConsole: ['...'], allowResponses: ['...'] })` — NOT RegExp (option-fixture values must serialize; RegExp breaks the worker boundary).
- **CI note (resolved)**: the `Pre-commit Checks` job used to fail on a cold cache because the now-**deleted** private `nikolay-e/pre-commit-hooks` repo couldn't be cloned. Both hooks (`no-emoji`, `shift-commit-time`) are now vendored as `repo: local` hooks under `.pre-commit-hooks/`, so the job passes on a cold runner and `docker-build-frontend` gates on it again. If a future hook references a private/deleted repo, vendor it the same way (don't re-add a PAT or decouple the gate).

## Image / cover-art subsystem (every `/qa` pass that touches covers)

The library has **music albums AND audiobooks**; their cover sources differ and must not be mixed. Read-path: `/Items/{id}/Images/Primary` resolves **image row → embedded art (own file, or album's first track) → 404** (`JellyfinMediaController`). So an album with embedded art OR a folder `cover.jpg` serves 200 even with an empty `images` row; only genuinely art-less items 404.

- **`images` table PK gotcha**: `images.entity_id` joins `entities.id` (NOT `entities.entity_id`). A join on the wrong column returns empty and falsely reads as "no images". Sanity: `SELECT count(*) FROM core_v2_library.images`.
- **Cover acquisition is a multi-source fallback** (`MetadataEnricher`): music albums → Cover Art Archive (by MB release-group) → **iTunes** (`media=music`) → **Deezer** (free-text, strong for RU/non-Western). Audiobook albums (`albumHasAudiobookTrack`) are **excluded from the music APIs** (they'd return a same-named music album's art) — their covers come from the dedicated per-track pass (borrow-parent → CAA-by-mbid → Open Library). All external clients throw `MetadataProviderUnavailableException` on 429/5xx so the entity is parked+retried, never burned.
- **The `metadata_checked_at` gate means images are NOT auto-re-derived.** "Burned" rows (`metadata_checked_at` set, no MBID) drop out of the candidate query forever. Detect: `SELECT count(*) FILTER (WHERE metadata_checked_at IS NOT NULL AND musicbrainz_id IS NULL AND release_group_mbid IS NULL) FROM core_v2_library.albums` (and artists). Re-queue after shipping a cover-source fix: `UPDATE ... SET metadata_checked_at = NULL WHERE ...` so the enricher re-runs the new chain.
- **Artist/cover caches MUST be persistent.** `backend.artistImages` + `backend.coverCache` were `emptyDir` (wiped on every pod restart) while the DB image rows persisted → broken artist images after any rollout. They're now mounted on the **media volume via `subPath`** (`.yaytsa/artist-images`, `.yaytsa/cover-cache`) when `media.enabled`, else emptyDir fallback. Album covers (written to `/media/<album>/cover.jpg`) were always persistent.
- **Legacy broken rows don't self-heal** (the candidate query treats any existing row as "has image", even if its file is gone). After making the caches persistent, one-time clean the stale ephemeral rows so the image-driven backfill re-fetches into the durable store: `DELETE FROM core_v2_library.images WHERE path LIKE '/app/artist-images/%' OR path LIKE '/app/cover-cache/%';`. Check distribution first: `SELECT count(*) FILTER (WHERE path LIKE '/app/%') FROM core_v2_library.images`.
- **Offline cover pre-cache**: the offline download path caches the cover by `coverKey` (album id) alongside the audio; it must NOT gate on a `Primary` tag (the backend often omits it on track items even when the album has art) — gating left every offline track coverless. A failed cover cache is best-effort `debug`, never error telemetry.
- **Browser verify**: on a populated page, `Array.from(document.querySelectorAll('img')).filter(i=>i.naturalWidth>10).length` should equal the img count, and a `client-errors`/`Images/Primary` network filter should show 0 beacons and 0 404s.

## Chart's own NetworkPolicy defaults to off — LLM-DJ silently dies with "Connection refused"

`charts/yay-tsa-v2/templates/networkpolicy.yaml` is a single comprehensive resource (Traefik ingress + DB egress + monitoring ingress + **LiteLLM egress**), gated behind `backend.networkPolicy.enabled` which **defaults to `false`** in the OSS chart (intentional: portability for third-party installs whose Traefik/Prometheus/DB live in different namespaces — see the chart's own values.yaml comment). Production's separate **central** `network-policies` gitops chart independently covers DB/traefik/monitoring for `yay-tsa` (so those kept working with the flag off), but has **no knowledge of the LLM-DJ feature** — there is no equivalent litellm-egress rule anywhere else. Net effect: `llm.enabled: true` + `networkPolicy.enabled` left at its default `false` = the backend's `default-deny-all` baseline blocks its own egress to `litellm:8080` with **"Connection refused"** (not a timeout — this cluster's CNI answers denied egress with an immediate refuse, not a silent drop), and `LlmClient` logs `LLM request failed: request failed` on every scheduler tick forever, degrading to ML-only exactly as designed — silent enough that it went unnoticed for the life of the feature. **Fix (shipped in gitops `values.prod.yaml`): explicitly set `backend.networkPolicy.enabled: true`** — this cluster's topology matches the chart defaults exactly (kube-system/monitoring/shared-database), so no namespace overrides were needed. Diagnose directly: `kubectl exec deploy/<backend> -- wget -T5 -qO- http://litellm.litellm.svc.cluster.local:8080/health/readiness` — "Connection refused" from inside the pod (despite litellm's own ingress NetworkPolicy correctly listing `yay-tsa`) means the gap is on the **egress** side, not litellm's ingress side (check `kubectl get networkpolicy -n <ns>` for a resource named after the app's own chart's fullname — if it's simply absent, the app-owned chart's NetworkPolicy was never rendered). Once fixed, `LlmClient`'s error signature changes from `request failed` (connection-level) to a real HTTP status (e.g. `HTTP 429`) — that transition is the proof the network layer is now fixed, independent of whatever the HTTP-level error turns out to be (a `429 insufficient_quota` from the upstream OpenAI-compatible provider is a **separate, billing-side** issue — out of scope for a code/infra fix, the graceful ML-only fallback is correct behavior for it).

## Long-lived Playwright MCP session + PWA service worker = false regression reports

A Playwright MCP browser tab kept open across an entire long QA session (spanning multiple deploys) can serve a **stale cached bundle** for `browser_navigate` even though `page.goto()` looks like a full reload — the PWA's service worker (NetworkFirst navigation, cached JS chunks) can satisfy the request from its own cache without ever re-fetching from origin. Symptom: a UI element that was just removed in a shipped fix (e.g., a duplicate search input from an old page component) still appears in `browser_evaluate`/snapshot DOM queries, looking exactly like a regression. **Before concluding a UI bug is real from a long-lived MCP session, always**: (1) curl the live bundle directly (`curl https://host/ | grep -oE 'assets/index-[^"]+\.js'` then `curl` that JS and grep for the suspect string) to get a cache-free ground truth from the server side, and (2) if the server-side artifact is clean but the browser still shows the old behavior, clear the browser's own cache before trusting it further: `await navigator.serviceWorker.getRegistrations().then(rs=>rs.forEach(r=>r.unregister())); await caches.keys().then(ks=>Promise.all(ks.map(k=>caches.delete(k))))`, then re-navigate. Don't file a bug from browser DOM state alone without this cross-check — it wastes time chasing a phantom regression in code that's already correct.

## ktlint requires one collection-literal item per line — manual multi-per-line edits fail CI even after a local `spotlessApply`

Hand-editing a Kotlin `listOf(...)` to add several new string literals in a compact multi-items-per-line style (e.g. `"a", "b", "c",` `"d", "e", "f",`) can pass a **stale/cached** local `spotlessApply` (if the module's Gradle task is up-to-date from before the edit) while genuinely violating ktlint's one-item-per-line rule for multi-line collection literals — CI's `spotlessKotlinCheck` (which always runs clean) then fails on a file that "was already formatted" locally. Don't trust a quick `spotlessApply` run right after a manual multi-line edit without also running `spotlessCheck` (which fails loudly on drift, unlike `Apply` which can silently no-op) — or just always run `./gradlew spotlessCheck` (whole project, cheap, all-UP-TO-DATE when nothing changed) as the last step before every backend commit, not just on the touched module.

## Stashing to unblock a cross-cutting pre-commit hook without touching someone else's WIP

`gitops`' `helm-lint` pre-commit hook lints **every** chart in the monorepo on every commit (documented generic pattern), so an unrelated, unfinished chart elsewhere in the working tree (someone else's in-progress `values.yaml`/new template with an undefined `.Values.x.enabled` reference) blocks an otherwise-valid commit to a _different_ chart. The safe fix is `git stash push --include-untracked -m "<label>" -- <path-to-the-unrelated-wip-dir>/`, commit cleanly, then `git stash pop` — never touch, complete, or delete someone else's uncommitted work to satisfy a hook; the pathspec-scoped stash is fully reversible and leaves it byte-identical once popped. If the local `main` also has a pre-existing **unpushed** commit unrelated to your fix, rebasing before push is normal (`git fetch && git rebase origin/main`, restashing/unstashing the same unrelated WIP around the rebase too) — but pushing someone else's unrelated unpushed commit bundled with yours needs a human confirmation call, it is not an automatic /qa action (shared production infra + ArgoCD `selfHeal:true` applies whatever lands on `main` immediately).

## (Fixed upstream 2026-07-01) ZAP false-positive WARN storm from bare-origin scanning — root cause was an autoqa bug, not app or config

A post-deploy-qa run showed `OK: no blocking findings` but carried 645 WARN-NEW ZAP findings (`Unexpected Content-Type` x611, `CSP: Wildcard Directive` x20, `COEP Header Missing` x14), every one at a bare `https://yay-tsa.com/v1/*` path (no `/api` prefix) despite `schemathesis-base-url: https://yay-tsa.com/api` being correctly configured in `ci.yml`. Root cause (confirmed by downloading the run's `autoqa-reports` artifact and inspecting `openapi-zap.json`'s rewritten `servers` block — it was `[{"url":"https://yay-tsa.com"}]`, the bare origin, not `.../api`): `nikolay-e/autoqa`'s `action.yml` "ZAP security scan" step never wired `QA_SCHEMATHESIS_BASE_URL` into its env block (only `QA_BASE_URL`), so `run-zap.sh`'s own documented fallback (`${QA_SCHEMATHESIS_BASE_URL:-${QA_BASE_URL}}`) silently always took the bare-origin branch — the sibling Schemathesis step wired it correctly, only the ZAP step was missing it. **This means WARN counts in the hundreds don't automatically mean "app problem" or "config problem" — check whether the tool itself is scanning the right target first** (`gh run download <id> -n autoqa-reports -D /tmp/x && jq '.servers' /tmp/x/openapi-zap.json` against what `schemathesis-base-url` was configured to). Fixed upstream: `nikolay-e/autoqa` commit `b874675` (issue [#18](https://github.com/nikolay-e/autoqa/issues/18)), pin bumped in `ci.yml` same day. If this WARN pattern reappears with the pin already current, it's a real finding — re-open triage.

## :app:test flake lottery — third pollution class: capped "full listing" assertions

Beyond the two 2026-06-21 root causes (auth bucket sharing, scheduled-scan reconcile), a third shared-context class surfaced 2026-07-03: a test asserting "all my seeded rows appear in the first page" of a size-capped listing (`search3` empty-query, `songCount=200` = `LibraryQueries.MAX_PAGE_SIZE`) breaks as soon as OTHER classes have seeded enough rows to push the seeds past the cap — order-dependent, passes in isolation, fails only in full CI runs (local order differed, so a green local `:app:test` did NOT disprove it). Fix pattern: page until exhausted (`songOffset += 200 while pageSize == 200`) and assert membership over the union — keeps the invariant, kills the order dependence. Any future "returns full library listing"-style assertion in the shared suite must page, never trust one capped page.

## nginx: server-level add_header does NOT reach proxied /api and /rest locations that include security-headers.conf elsewhere

ZAP flagged `Strict-Transport-Security Header Not Set` on all `/api/v1/*` 401s: HSTS lived only in `infra/nginx/security-headers.conf`, which is `include`d per static location — the proxied `/api/`, `/rest/`, stream and auth locations define no `add_header`/include of their own, so they inherit the SERVER-level header set, which lacked HSTS. Fix: add the HSTS `add_header ... always` at server level (locations with their own add_header keep using the conf include; locations without inherit server level). Verify: `curl -sI https://yay-tsa.com/api/v1/time | grep -i strict` after the frontend image rolls.

## ZAP SQLi HIGH on the login path = AuthRateLimitFilter 429, downgrade via zap-rate-limited-paths

After the failed-auth rate limiter shipped, ZAP's boolean-SQLi probe chain on `POST /api/Users/AuthenticateByName` trips the 10-failures/min bucket; the 401→429 response flip reads as injection evidence (HIGH, blocking). Not an app bug (BCrypt + parameterized JPA; the probe never reaches SQL). Fix in one place: `zap-rate-limited-paths: '/api/Users/AuthenticateByName'` on the autoqa step (autoqa #7 downgrades HIGHs confined to declared rate-limited paths to info). If a SQLi HIGH ever fires on a path NOT declared there, it is a real finding.

## Cloudflare QUIC + EventSource: recurring benign console errors

Long-lived `EventSource('/api/v1/me/devices/events')` over HTTP/3 through Cloudflare logs `net::ERR_QUIC_PROTOCOL_ERROR` console errors whenever the stream dies (backend rollout, CF QUIC idle rollover); the hook reconnects and device sync keeps working (verify: a fresh EventSource `onopen` fires <5s). Not app-fixable (EventSource can't pick transport), does NOT reach client-error telemetry (resource-load class is dropped). Don't file these from browser QA; only investigate if a fresh SSE fails to OPEN.

## SonarCloud backlog note (2026-07-03)

Quality gate OK. Standing accepted classes keep accruing with new code: S3735 `void` (deliberate no-floating-promises pattern — never "fix"), S1607 on live-backend e2e specs (conditional skips by design). Fixed-this-pass: S7721 (hoist `resolveNormalizationGainDb`), S3776 (hotkey handler flattened to a resolver map). Remaining ~50 pre-existing cosmetic issues (S3358 nested ternaries in DownloadButton, S6582 optional chains in audio-offline-sw.js, S3776 in api.client/PlayerBar/offline.store, S8786 regex in audio-ml) are backlog, tracked here — none new, none gate-blocking.

## Stale-chunk 404 during frontend rollout — vite:preloadError auto-reload

nginx logs `open() "/usr/share/nginx/html/assets/<name>-<hash>.js" failed (2: No such file or directory)` for a few minutes around every frontend rollout: a browser tab that loaded `index.html` from the OLD build still references old content-hashed chunk names, and once the new pod replaces the old files, a lazy-route `import()` for one of those chunks 404s. This is normal SPA-during-deploy behavior, not a bug — but it USED to leave the user stuck on `ErrorBoundary`'s "Something went wrong" screen, because the "Try again" button just re-renders the same component, and a Vite dynamic-`import()` promise rejection is cached forever (retrying never re-fetches). Fixed: `installChunkReloadRecovery()` (`apps/web/src/app/infra/chunk-reload.ts`) listens for Vite's native `vite:preloadError` window event and does a guarded one-time `location.reload()` (sessionStorage guard, auto-clears after 5s so a later independent deploy can still trigger recovery). `ErrorBoundary`'s retry button remains correct for ordinary (non-chunk) render errors — no change needed there. Verify after any nginx-404-chunk sighting in K8s logs: confirm it's within a rollout window (frontend image tag changed within the last few minutes) and that no user reported a stuck screen — the new listener should have handled it silently.

## Single-node k3s: transient DiskPressure taint during image pulls

A backend/frontend rollout pulling a new image can occasionally trip `FailedScheduling ... untolerated taint {node.kubernetes.io/disk-pressure}` for a few minutes on this single-node cluster — `kubectl get events` shows it, but `kubectl get node <name> -o jsonpath='{.status.conditions[?(@.type=="DiskPressure")]}'` clears to `False`/`KubeletHasNoDiskPressure` within minutes and the pod schedules successfully once it does. Not app-fixable (no second node to spread the pull), not a real problem unless the condition stays `True` for an extended period — only escalate if a pod stays `Pending` past ~10 minutes with this taint still active.

## Observed: a push-triggered CI run can get `cancelled` with zero jobs before a newer push, despite `cancel-in-progress: false`

`ci.yml`'s concurrency block is `cancel-in-progress: ${{ github.event_name == 'pull_request' }}` (explicitly false for `push`, per the comment: "queue pushes to main… cancelling after image push but before post-deploy QA would deploy unverified code"). Observed once: a run sat for ~7 minutes with the Actions jobs API reporting `{"total_count":0,"jobs":[]}` the whole time, then flipped to `conclusion: cancelled` within seconds of a newer commit landing — before ANY job (not even `pre-commit`) had started. Net risk is zero (no image ever pushed, nothing deployed) since cancellation happened at the pre-job/queued stage, consistent with the safety comment's actual concern. Cause not confirmed (GitHub-side queueing behavior for back-to-back pushes to the same concurrency group, observed on this repo's Actions runner pool) — if a run gets cancelled AFTER `docker-build-frontend`/`docker-build-audio-separator`/`v2-ci` image push but BEFORE `post-deploy-qa` completes, that WOULD be the unverified-deploy risk the comment warns about and needs immediate investigation; a same-run zero-job cancellation pre-job-start does not.

## ZAP SQLi false positive is a generalizable class — use path PREFIXES, not one path per finding

Every `principal: Principal`-parameter Spring MVC method (Spring Security requires an authenticated context to resolve `Principal`, so unauthenticated requests 401 in the filter chain before the handler runs) produces the identical ZAP boolean-SQLi false-positive shape on ANY fuzzed input: a consistent status code regardless of payload reads as injection evidence. Fixing one path at a time as ZAP's random walk happens to hit it (`AuthenticateByName` → `heartbeat` → `Sessions/Playing/Progress`, three separate passes) is reactive. Once a controller's Kotlin source confirms EVERY method under a path takes `Principal`, add the prefix (`/api/v1/me/devices`, `/api/Sessions/Playing`) to `zap-rate-limited-paths` in one shot instead of the single leaf path — verify via `grep -n "@.*Mapping\|Principal" <Controller>.kt` before widening, so the prefix never accidentally covers a `permitAll` endpoint.

## Docker Desktop not installed locally → `actionlint-docker`/`hadolint-docker` hang forever, not fail fast

On this machine, `docker info` hangs indefinitely (no daemon, app not installed) rather than erroring — so the two Docker-backed pre-commit hooks (`actionlint-docker`, `hadolint-docker`) silently stall a `git commit` past any reasonable timeout instead of failing with a clear message. Diagnose with `docker info` directly (also hangs) and `ls /Applications/Docker.app` (confirms not installed, vs. just not running). Legitimate one-off workaround: `SKIP=actionlint-docker git commit ...` (only for workflow-YAML-only changes with no new GHA expression syntax) — CI's own `pre-commit` job runs on a GitHub-hosted runner where Docker is always available, so the real gate still fires on push. Never blanket-`SKIP` both hooks preemptively; only skip the specific one blocking the specific commit, and only when Docker is confirmed absent (not just slow).

## post-deploy-qa expected-backend-SHA must come from the GHCR registry, not `git log`

`docker-build` (`.github/actions/docker-build`) tags the backend image `main-<github.sha[:7]>` — the **push HEAD**, NOT the last backend-touching commit's own sha. `v2-ci` triggers on a push whose _range_ touches `yay-tsa-v2/**`, so a push that bundles a backend change with trailing docs/frontend commits (or where a backend commit isn't the tip) publishes `main-<HEAD>`. The old `Resolve expected backend SHA` step in `ci.yml` used `git log -1 --format=%H -- yay-tsa-v2 charts/yay-tsa-v2 .github/workflows/v2-ci.yml`, which returns the _inner_ backend commit's sha — so it waited for `main-<inner>` while the registry only had `main-<HEAD>`, and `wait-backend-rollout` timed out ("Backend never settled on main-<sha>"), reddening post-deploy-qa on **every subsequent non-backend push** (the live backend tag stays at the last bundle's HEAD forever). Fix (shipped): resolve from the registry — the newest `main-<sha7>` tag on the backend package IS exactly what v2-ci published and Image Updater is rolling out:
`gh api "/users/${OWNER}/packages/container/yay-tsa%2Fyay-tsa-v2%2Fbackend/versions" --jq '[.[].metadata.container.tags[]? | select(test("^main-[0-9a-f]{7}$"))][0]'` (needs `packages: read` on the job), with git-history + `github.sha` fallbacks. Verify a suspected recurrence with `curl -s https://yay-tsa.com/api/System/Info/Public | jq .BuildSha` vs the registry's newest tag — if they agree but `git log -1 -- <backend paths>` differs, it's this class.

## resume_position write was a merge (read-then-insert) → duplicate-key race under concurrent first play

`JpaResumePositionRepository.save` did `jpa.save(entity)`; the entity has a composite `@IdClass` (user_id, item_id), so Spring Data `merge()` runs SELECT-then-INSERT/UPDATE. Two concurrent FIRST progress reports for the same (user, item) both saw an empty row and both INSERTed → `duplicate key value violates unique constraint "resume_position_pkey"`, logged at ERROR by Hibernate (the recorded position was lost; `JellyfinSessionsController` catches it as a WARN so the user isn't broken). Rare (only the first write for a track, under concurrency). Fix (shipped): native `INSERT ... ON CONFLICT (user_id, item_id) DO UPDATE` on the JPA repo, `@Modifying(clearAutomatically = true, flushAutomatically = true)` + `@Transactional` — clearAutomatically is REQUIRED because `@DataJpaTest` runs one persistence context per test, so a native upsert leaves the managed entity stale and a later `find()` reads the pre-upsert row (the existing `ResumePositionServiceTest` re-reads after every write). Detect: `kubectl logs deploy/yay-tsa-v2-production-backend --since=1h | grep -c resume_position_pkey` should be 0.

## Favorite/star returned 409 on every re-favorite — OCC save assumes bump-by-1, handler no-op keeps the version

`PreferencesHandler.setFavorite` treats an already-favorite track as a true no-op (`snapshot.asSuccess(snapshot.version)`, version UNCHANGED). But `JpaUserPreferencesRepository.save` runs OCC as `UPDATE ... SET version = :new WHERE version = :new - 1` (`aggregate.version.value - 1`), hard-assuming every Success bumped the version by one. A no-op then matches zero rows → `OptimisticLockException` → `Failure.Conflict` → **HTTP 409 on every idempotent re-favorite / OpenSubsonic re-star**. The PWA toggles on known state so it rarely double-POSTs, but any client that re-stars (Symfonium/Feishin sync) or a retried POST hits it. Fix (shipped): `PreferencesUseCases` persists only when `result.newVersion != snapshot.version` — a no-op skips save + idempotency-store + outbox entirely. **General class: a domain no-op that returns Success with an unchanged version is incompatible with an OCC repo that derives `expectedVersion = newVersion - 1`.** Detect black-box: `curl -X POST /api/UserFavoriteItems/<already-fav-id>` twice — a 409 on the second is this bug (should be 200). Regression test in `FavoritesConsistencyIntegrationTest`.

## #266 was fixed for albums/artists but the tracks (Songs "Recently Played") leg was left dead

The personalized `/Items?IncludeItemTypes=Audio&SortBy=DatePlayed` branch still guarded on `excludedGenres.isEmpty()` after the album/artist legs were fixed — and the PWA ALWAYS sends `ExcludeGenres=audiobook,audiobooks`, so the Songs personalized order never fired for any real user (silently degraded to the deterministic full-library browse). `buildPersonalizedTracks` already excludes audiobooks/red-lines via `musicSurfaceFilter`, so the fix drops the guard and threads `excludedGenres` through (per-track primary-genre match, on top of musicSurfaceFilter) to also honor arbitrary client exclusions. Black-box distinguisher (the branch reports its OWN page size as TotalRecordCount, the fallback reports the full count): `curl '/api/Items?IncludeItemTypes=Audio&SortBy=DatePlayed&StartIndex=0&Limit=5&ExcludeGenres=audiobook,audiobooks'` — `TotalRecordCount=5` means the personalized branch fired; `TotalRecordCount=<full-library>` (e.g. 7179) means it's still the dead fallback.

## Schemathesis "Missing authentication: N operations returned only 401/403" has two known non-app causes

Both look like a broken app but are harness-side; check them before touching backend auth:

1. **Invalid Origin from autoqa** (fixed upstream in `b783d88`, issue autoqa#25): schemathesis sent `Origin: <base-url>` verbatim, and a base URL with a path (`https://yay-tsa.com/api`) is not a valid Origin → Spring CORS 403 `Invalid CORS request` (text/plain) on EVERY request → 77 pseudo-failures (JSON deserialization / Missing Content-Type). Signature: failure bodies are all `Invalid CORS request`.
2. **Fuzzer self-logout**: with a valid Origin the fuzzer's `POST /Sessions/Logout` succeeds against its own live Bearer token and revokes it mid-run — every later operation 401s (the 502-rollout blips then become the only "failures" left). Fix (shipped): `/Sessions/Logout` and `/Users/Password` in `schemathesis-exclude-paths`. **General rule: any session-terminating or credential-mutating operation must be excluded from token-authenticated fuzzing** — it self-DoSes the run (and Password risks locking the shared QA account; it survived here only because the endpoint demands the current password in the body). Both stay covered by integration tests + the crawler's real login.

## detekt is live again build-wide (2026-07-07, closes #267)

`./gradlew build` → `check` → `detekt` now runs on all 26 modules. Three pieces hold it together — breaking any of them regresses silently:

- **Kotlin pin in afterEvaluate** (root `build.gradle.kts`): Spring's dependency-management BOM downgrades kotlin-compiler-embeddable on the `detekt` configuration ("compiled with Kotlin 2.0.x but running with 1.9.x", detekt/detekt#7384). The `resolutionStrategy.eachDependency` pin MUST be registered inside `afterEvaluate` — eachDependency rules run in registration order and the last useVersion wins; outside afterEvaluate the BOM's rule lands later and re-breaks the 4 `infra-persistence:*` modules.
- **PascalCase naming allowance** (`detekt.yml` VariableNaming/ConstructorParameterNaming): Jellyfin wire DTOs/params are PascalCase by protocol; don't "fix" them.
- **Frozen per-module `detekt-baseline.xml`** (198 pre-existing findings): the gate fails only on NEW findings. Regenerate with `./gradlew detektBaseline` ONLY to shrink after real cleanups — regenerating to absorb new findings defeats the gate.

## dnd-kit keyboard-reorder e2e: the 200ms sleeps are load-bearing, don't "fix" S2925 there

The keyboard sensor measures droppable rects asynchronously after pickup with NO DOM observable; an ArrowDown arriving before that silently does nothing. Every condition-based sync tried (live-region announcements — race to overwrite; translate3d displacement + retry-press) flaked ~25% under `--repeat-each=6` while the original sleeps pass 12/12. The four playlists S2925 are **accepted in SonarCloud with this justification** — new `waitForTimeout` in OTHER specs is still a finding (chunk-reload uses `waitForEvent('load', {timeout})` for its negative assertion, audiobooks uses `expect.poll` on `audio.currentTime` — both genuinely better).

## Browser-QA: a "spontaneous pause/skip" is usually a legitimately short track — check metadata before calling it a bug

A track that "skips mid-play after 12s" or "pauses itself at 34s" reads as a playback bug but both signatures resolved to natural ends: the library has real 12–55s interludes, and `Seamless switch complete {seekPosition: 0}` in console = normal gapless advance. Verify before filing: (1) the playing element's `duration` vs the item's `RunTimeTicks` (agreeing short values = short track), (2) offline-cache integrity via IndexedDB `blobs` size vs server `HEAD /api/Audio/{id}/stream` Content-Length (byte-equal = intact; don't infer truncation from bitrate guesses — the library mixes FLAC ~117KB/s and MP3 320 ~40KB/s). Also: `document.querySelector('audio')` samples only the FIRST of the dual elements — during/after a gapless advance the active one is often the second; enumerate both.

## Client-error telemetry review must filter the QA run's own traffic first

The monkey/crawler generates real client-error beacons (UA `Chrome / Linux`, timestamps inside the post-deploy-qa run window, version = the tag under test). In one 24h window ALL ~70 entries were monkey-generated (56× "Offline download failed" from navigation-aborted background listening-cache downloads — since downgraded to debug for auto-download reasons `listening-cache`/`favorite`, warn kept for user-initiated). Correlate `sid`/UA/time-window against `gh run list` before treating a fingerprint as a real-user defect.

## Shared-context :app:test seed with a hardcoded UNIQUE column collides the moment a 2nd test method is added

`HttpIntegrationTestBase` subclasses share one Postgres and commit without rollback, so `@BeforeEach` seed rows accumulate across every test method AND class. A seed that inserts a hardcoded value into a UNIQUE column works only while the class has a single test — adding a second method re-runs `@BeforeEach` and the second insert violates the constraint. Hit this pass on `FavoritesConsistencyIntegrationTest.seed()` using `source_path = "Fav/Album/$idx.flac"` → `duplicate key ... entities_source_path_key` on the second test's seed. Same fixture-isolation class already documented for usernames — key every seeded UNIQUE column on a per-invocation UUID (`"Fav/$id.flac"`, `id` = the fresh per-track UUID). The failing test's own body is a red herring; read the test-report XML artifact (`gh run download <id> -n v2-test-reports`) for the real constraint name — Gradle's console summary prints only the exception class + line (the seed line), not the SQL detail.

## MCP OAuth flow + .well-known routing (2026-07-09)

The claude.ai MCP connector needs OAuth discovery at the domain root. Two routing layers exist; only ONE works on this cluster: the chart's Traefik `wellknown` Ingress (priority 30) never matches because the PWA catch-all wins (same class as the documented `/rest` case) — the WORKING route is the frontend nginx `location ^~ /.well-known/oauth-` proxy in `infra/nginx/nginx.conf.template`. When an OAuth discovery 404s, check WHICH server answered (`server: nginx` + HTML = frontend static lookup, problem+json = backend) before touching the backend. OAuth endpoints are deliberately hidden from OpenAPI (`oauthPathsHiddenCustomizer`) — fuzzing unauthenticated DCR pollutes `oauth_clients`; they're covered by `McpOAuthFlowIntegrationTest`.

- **Cloudflare blocks POSTs with `Python-urllib` UA (403 before origin)** but passes `python-httpx`/browser UAs. Scripted QA of yay-tsa.com POSTs must set a browser-ish UA or the flow falsely "breaks".
- **`playback_sessions` empty ≠ broken MCP**: until 5d7adf9f the PWA never populated the playback aggregate (all local). `ReflectExternalPlayback` now mirrors `/Sessions/Playing[/Progress|/Stopped]` into the session (lease → reporting device, change-gated: track/state change, >5s drift, lease <2min). Verify live: play in the PWA, `GET /api/Sessions` must show `NowPlayingItem` within ~10s. A session leased by ANOTHER device rejects reflection by design.
- **`current_entry_id`/`queue_entries` id columns are varchar(36)** — queue entry ids must be UUID-sized (the reflected entry id IS the track UUID). A longer id fails only at flush with `value too long`, surfacing as InvariantViolation from the use case.

## jackson-module-kotlin null-in-collection (2026-07-09)

Without strict null checks a JSON `null` inside a non-nullable `List<String>` body deserializes silently and NPEs inside the controller (`Parameter specified as non-null is null` from kotlin intrinsics) → 500. Fixed twice-over: `JacksonConfig` enables `KotlinFeature.StrictNullChecks` (NB: `NewStrictNullChecks` needs jackson-module-kotlin 2.19+, catalog pins 2.18 — local warm-daemon builds compiled the wrong name "successfully", only `--rerun-tasks`/clean CI caught it), and `GlobalExceptionHandler` maps read-side `HttpMessageConversionException` → 400 (write-side `HttpMessageNotWritableException` stays 500). Regression tests: `PlaylistsIntegrationTest` null-in-Ids → 400 problem+json.

**Gradle warm-daemon trap:** a `./gradlew build` right after creating a NEW source file can report green without actually compiling it (observed twice this pass). Before trusting a green run that gates a push, force the changed module: `./gradlew :module:compileKotlin --rerun-tasks` — or rely on clean CI only.

## Remote-command targeting: the client id must be the TOKEN-BOUND one (2026-07-10)

Remote commands (SSE `command` events, MCP/publish-only path) target the deviceId bound to the auth token (`DeviceBoundAuthentication`), which is what reflection/lease/heartbeat-fallback all use server-side. The PWA's locally generated `yaytsa_device_id` can DRIFT from it on legacy persisted sessions — the fail-closed `targetDeviceId` check then silently drops every command with zero errors anywhere. Since 909cd218 the heartbeat echoes the registered identity (`{deviceId, sessionId}`) and the client adopts it (`features/player/device-identity.ts: getEffectiveDeviceId()`). When "remote command has no effect": subscribe `curl -N /api/v1/me/devices/events?api_key=…`, fire the command, compare the event's `targetDeviceId` against the browser's `getEffectiveDeviceId()` — a mismatch is this class, not an SSE-delivery problem.

## Schemathesis conformance levers live in app/OpenApi\*Config, not adapter annotations

Adapters have no springdoc/swagger/jakarta-validation deps by design — add spec constraints programmatically in `app`: `OpenApiConfig` (global error codes, path-param patterns/uuid, query int bounds, oauth hiding) and `OpenApiConformanceConfig` (body-field minLength/uuid maps, path int bounds, multipart required). Root causes fixed this pass, all pre-existing: untyped `List<Map<String,Any?>>` responses emit `additionalProperties:{type:object}` that string values violate (type a DTO); Kotlin DTOs with PascalCase property names emit DUAL schema properties (writeOnly PascalCase + camelCase) while Jackson accepts only the constructor name — always camelCase props + `@JsonProperty("PascalCase")` + `@JsonAlias("camelCase")`; `format: int32` alone does NOT bound schemathesis generation (format is annotation-only) — explicit minimum/maximum required.

- **detekt baselines key on signatures** — a type-only change to a grandfathered function invalidates its baseline entry; regenerate with `./gradlew :module:detektBaseline` (1-line diff), don't refactor under time pressure.
- **MCP tools are E2E-testable via plain `POST /api/mcp`** (JSON-RPC `tools/call`) with the QA account's Bearer token — no claude.ai connector needed, keeps the no-admin rule intact.
- **Transient 502s in schemathesis output (≈2/6500 requests, Cloudflare HTML body)** are CF↔origin blips — reproduce with the log's curl before chasing; a clean 405/400 problem+json on replay closes them.
- **`docker buildx` "failed to fetch oauth token … auth.docker.io … 502"** in image-build jobs is Docker Hub flake — `gh run rerun <id> --failed`.

## A long @Scheduled job silently freezes SSE delivery (outbox poller starvation)

Spring Boot's default TaskScheduler is ONE thread. `KaraokeProcessor` used to run multi-minute separations inline in its @Scheduled tick — a 50-track batch occupied `scheduling-1` for hours, and the outbox poller (1s tick) simply never ran: remote commands and device-state events piled up in `core_v2_shared.outbox` with `published_at IS NULL` while every HTTP surface stayed green. Diagnose in one query: `SELECT context, published_at IS NULL, count(*) FROM core_v2_shared.outbox GROUP BY 1,2` — unpublished `device-command` rows with a healthy pod = a starved scheduler, not an SSE problem. Fixed 39a84d84: separation moved to a dedicated `karaoke-separation` thread with an in-flight guard, plus `spring.task.scheduling.pool.size: 4` as defense. Any future long-running @Scheduled work must follow the same dispatch-to-own-executor pattern.

- **Bulk karaoke requeue is a load event**: 723-track backlog keeps the GPU sidecar at ~1 CPU for hours; Post-Deploy QA runs in that window can catch transient CF 502/520s that don't reproduce on replay. Requeue in bulk only after confirming the batch runs off-scheduler, and expect the first QA run after to be noisy.

## MCP OAuth: a strict edge form-action CSP silently blocks the claude.ai connector

The /oauth/authorize consent POST 302s the browser to the client's registered redirect_uri (Claude hosted = `https://claude.ai/...`, Claude Code = loopback `http://127.0.0.1:*`/`http://localhost:*`), and Chrome (~v120+) enforces `form-action` against the WHOLE redirect chain — a bare `form-action 'self'` blocks the redirect and the connector can never finish authorizing (browser console: "Sending form data to '…/api/oauth/authorize' violates … form-action 'self'"). Symptom looks like "new MCP tools not appearing" because the user can't reconnect to refresh the tool list.

- **Where the CSP actually comes from (yay-tsa.com):** NOT the backend (`OAuthHtmlPages` sets `default-src 'none'` but it's overridden) and NOT the v2 Traefik `secure-headers` middleware (HSTS only). When the origin sets NO CSP, **Cloudflare injects a default** `…form-action 'self'…` on every response; when the origin DOES set a CSP, Cloudflare passes it through unchanged (proven live: `mcp.life-as-code.com`, same CF zone, serves its Traefik-set `form-action 'self' https://claude.ai …` to the browser). So the fix must set the CSP at the **origin** (Traefik middleware), which then wins at the edge.
- **Fix (charts/yay-tsa-v2):** added `ingress.secureHeaders.contentSecurityPolicy` with `form-action 'self' https://claude.ai http://127.0.0.1:* http://localhost:*` to the v2 `secure-headers` middleware (already attached to /api, /rest, /.well-known). Mirrors the proven `life-as-code` `mcp-headers` middleware. CSP is inert on JSON/XML API responses; it only governs the OAuth HTML pages.
- **Defense-in-depth (backend):** `OAuthHtmlPages.securityHeaders(redirectUri)` now emits `form-action 'self' <registered-redirect-origin>` — correct even if a route ever bypasses the edge middleware. `redirect_uri` is validated against the registered client BEFORE the page renders, so the reflected origin is never attacker-controlled.
- **Diagnosis shortcut:** `kubectl get middleware -A -o …` then grep `contentSecurityPolicy` for `form-action` — the working `life-as-code/mcp-headers` is the reference. Verify the fix live with `curl -sI https://<host>/api/oauth/authorize?... | grep -o "form-action[^;]*"` — it must list the client origin, not bare `'self'`.
- **Connector tool-list cache:** claude.ai caches the MCP `tools/list` at CONNECT time and only refreshes on reconnect (the server doesn't push `notifications/tools/list_changed` for a static list). After deploying new tools, the user must reconnect the connector (or start a new chat) to see them — "old surface" in an existing chat is a stale client cache, not a rollback. This is why the form-action bug is doubly painful: it blocks the very reconnect that would surface the new tools.

## /api routing priority: the frontend catch-all wins, so /api gets the SPA's CSP (not v2's)

The `yay-tsa-v2-production-api` ingress sets `traefik.ingress.kubernetes.io/router.priority: "10"`, but the frontend `yay-tsa-production` ingress (`PathPrefix(/)`, carrying `kube-system-security-headers`) has a higher default (rule-length) priority. So `/api/*` requests match the FRONTEND router, flow through the frontend nginx (which proxies /api to the v2 backend), and pick up the shared kube-system CSP with `form-action 'self'` — NOT the v2 `secure-headers` CSP. Consequence: a CSP fix on the v2 middleware has ZERO effect on /api responses (that router never wins). This is why the MCP OAuth form-action block persisted after editing v2 `secure-headers`.

- **Diagnose:** `curl` the origin bypassing Cloudflare — `kubectl run tmp --image=curlimages/curl -- curl -sik -H "Host: yay-tsa.com" https://<traefik-clusterIP>/api/oauth/authorize` — if it still serves the kube-system CSP, the v2 router isn't being selected. Confirm with `kubectl get ingress <api> -o jsonpath='{...router.priority}'` vs the frontend ingress (no explicit priority = length-based, ~40).
- **Fix pattern:** a dedicated ingress for the specific path that must reach the v2 backend directly, with `router.priority: "100"` (clearly above the frontend) + `strip-api` + `secure-headers` middlewares. `charts/yay-tsa-v2/templates/ingress-oauth.yaml` does this for `/api/oauth`. Only routes that genuinely need the v2 CSP / direct backend path should get this — everything else keeps flowing through nginx (which does strip-prefix, audio X-Accel-Redirect, etc.), so don't blanket-raise the whole /api priority.
- The v2 `secure-headers` middleware DOES carry the MCP CSP (`ingress.secureHeaders.contentSecurityPolicy` in values) — necessary but not sufficient until a router that uses it actually wins for the path.

## A missing multipart part is a 500 unless mapped

A malformed multipart upload (mismatched boundary between Content-Type and body, or an empty part name) parses to zero parts, so a required `@RequestPart("file")` throws `MissingServletRequestPartException` — which is NOT `MultipartException` and, without its own `@ExceptionHandler`, falls through to the catch-all 500. Schemathesis flags this as a real `[500]` server error on `POST /tracks/upload`. Map `MissingServletRequestPartException` → 400 in `GlobalExceptionHandler` (alongside `MissingServletRequestParameterException`). The multipart parser runs in the servlet filter chain BEFORE the controller's `requireAdmin()`, so even a non-admin token reaches the 500 — auth does not shield it.

## Poison signal rows crash AffinityAggregator with "integer out of range"

A `playback_signals` row whose `context` JSON carries an absurd `positionMs` (e.g. `2036265434362` ≈ 64000 years — a fuzzer artifact or a timestamp mistaken for a position) makes `AffinityAggregator`'s `SUM(positionMs/1000)` overflow the int4 `total_listen_sec` column every 60s tick — the whole affinity pipeline stops updating (LLM-DJ taste degrades) while HTTP stays green. Guard both ends: clamp `positionMs` at ingestion (`ScrobbleService`) AND in the aggregate SQL (`LEAST(…, 86400000)` + bounded regex `[0-9]{1,9}`) AND in `ListeningStatsService`. Live repair without waiting for deploy: `UPDATE core_v2_adaptive.playback_signals SET context = regexp_replace(context::text, '("positionMs"\s*:\s*)[0-9]{13,}', '\1' || '86400000') WHERE length(substring(context::text from '"positionMs"\s*:\s*([0-9]+)')) > 12;` — the aggregator recovers on its next tick.

## Residual schemathesis conformance findings (post fixes)

After the body-field / path-param / int-bound customizers, the recurring post-deploy-qa schemathesis failures reduce to a small tail. Fixable classes and their fixes (all in `app/OpenApiConfig`/`OpenApiConformanceConfig`):

- **Undocumented 206 on Range GETs** (`/Audio/{id}/stream`, `/Karaoke/{id}/instrumental|vocals`): a `Range` request answers 206, which spring-doc never infers. `partialContentResponseCustomizer` declares 206 on all GET operations (harmless on non-Range GETs — a documented-but-unused status is not a failure).
- **int64 body overflow** (`PositionTicks` etc.): the int-body customizer only capped `format: int32`; int64 fields had no maximum, so a huge fuzz value overflowed Long → 400. Now capped at `Long.MAX_VALUE` for non-int32 integer fields.

Genuinely-not-worth-fixing tail (documented, left as-is): a fuzzer sending a **whitespace-only** string to a field whose backend does `isBlank()` rejection (`name: "\t"` on POST /v1/groups; `command: ""` on the device command) — the JSON-schema `minLength: 1` passes a single whitespace char but the handler requires non-blank. Expressible only via a `\S` pattern per field; low value, high annotation churn. These are the expected "API rejected schema-compliant request" residue, not server bugs (clean 400 problem+json). Do NOT chase them into per-field regex patterns.

## Internal autoqa races the single-replica rollout → transient CF 502s (post GHA-migration)

Since image builds + post-deploy QA moved off GitHub Actions onto the in-cluster Argo pipeline (2026-07-11), the autoqa Sensor fires **immediately** on the Forgejo push webhook — it no longer inherits the ~5min GitHub reverse-sync head-start the old GHA `post-deploy-qa` had. The backend is `replicas: 1` (HARD INVARIANT — per-JVM state), so every rollout has an unavoidable window where the old pod is gone and the new Spring Boot pod is still starting (Flyway + Tomcat, ~40s), during which Cloudflare's edge returns its own `502: Bad gateway` HTML page for every request. If an autoqa run overlaps that window (especially under rapid back-to-back pushes, each triggering its own build+deploy+autoqa), it records the 502s as failures:

- **schemathesis** → "Undocumented HTTP status code: Received 502" on many operations (the 502 body is Cloudflare HTML, `server: cloudflare`, not a backend response).
- **zap** → "zap-report.json missing — ZAP step failed to produce report" (its OpenAPI import fetch got a 502, so no scan ran).
- **monkey** → dozens of non-blocking "failed request" findings (same 502s).

**These are NOT backend bugs.** Confirm the transient nature before chasing any of them: re-hit the flagged endpoints against the settled backend — they return documented statuses (e.g. DELETE `/Admin/Users/{id}` → 403, DELETE `/Items/{id}` → 404), and `kubectl get pods -n yay-tsa` shows the backend with **0 restarts** (no OOM/crash — the transient is the rollout, not the app). The decisive re-verify is a fresh autoqa run submitted only when no rollout is in flight (`kubectl get deploy -n yay-tsa` image tags all == target SHA, pods stable ≥1min).

Distinguish from the _load_-induced CF 502/520 already documented under the karaoke-requeue note: that one is GPU-saturation during a bulk backlog; this one is the deploy window itself. Both share the Cloudflare-edge-502 symptom and the same "verify on replay" remedy. A durable fix belongs upstream in `nikolay-e/autoqa` (tolerate/retry edge 502s whose body is the Cloudflare challenge/error page, distinguishable by `server: cloudflare` + the `502: Bad gateway` title) rather than per-app config — tracked as a GH issue, not worked around by excluding routes.

## QA test account password is shared with the internal autoqa secret

The `test` user's password (Keychain `yay-tsa-qa-password`, used by browser QA) is now the SAME value as the in-cluster `autoqa-crawler-login` secret (namespace `argo-workflows`, key `password`) that the internal autoqa injects into its `auth-body` — aligned 2026-07-11 so both the browser-driven QA and the pipeline authenticate as `test`. If either is rotated, rotate BOTH and re-hash the DB row: `UPDATE core_v2_auth.users SET password_hash='<bcrypt-cost-13>' WHERE username='test' AND is_admin=false` (backend uses BCrypt cost 13). Never point QA at `master` (admin).

## Scanner cover delete+insert trips idx_images_one_primary → whole track upsert aborts → ~40% of the library 404s on stream (2026-07-12)

The single highest-impact class a `/qa` browser/stream pass catches. Symptom: `curl /api/Audio/{id}/stream?api_key=… -H 'Range: bytes=0-127'` returns a **clean `404 application/problem+json`** for a large fraction of real tracks (sampled ~40% one pass), while `/Items` lists them and `/health` is green. Root cause is NOT the stream path — it's the **library scanner silently failing to persist those tracks**, so the DB keeps a stale v1-ETL row whose `source_path` no longer matches disk (e.g. DB `Moonspell/1998 - Sin.Pecado/Abysmo.flac` vs disk `…/04 - Abysmo.flac`; every row dated the ETL day `2026-05-16`), and the resolved file 404s.

Mechanism: `LibraryWriter.ensureAlbumCover` / `ensureAudiobookTrackCover` replaced an entity's Primary image via `imageRepo.delete(existing)` then `imageRepo.save(new)`. Hibernate orders **inserts before deletes** within a flush batch, so the new Primary INSERT executes before the old Primary DELETE → both momentarily present → violates the `idx_images_one_primary` unique index (V006). The bad insert is _deferred_ and only flushes when the next native `@Modifying` query runs (`genreRepo.upsertByName` in `linkGenre`), so the stack trace **blames `linkGenre:404` — a red herring**; read the full exception, the failing SQL is `insert into core_v2_library.images … is_primary=TRUE … duplicate key … idx_images_one_primary`. The `DataIntegrityViolationException` aborts the whole `@Transactional upsertTrack`, so the track's correct-path row is never written. Triggers on every album whose existing Primary row has a stale path (moved/renamed cover, v1-ETL cover-cache paths) — i.e. most of the ETL library.

Fix (shipped `56cf690d`): `imageRepo.delete(existing); imageRepo.flush();` before the `save(new)` at BOTH cover sites — the explicit flush forces the DELETE to hit the DB before the INSERT. Regression test `LibraryWriterHygieneTest."scanning a track whose album has a stale-path Primary image replaces it without aborting the track"` (jdbc-corrupts the album's Primary path, rescans, asserts the second track persists + exactly one Primary) — verified it FAILS without the flush. Post-deploy the scheduled scan self-heals: it processes the previously-failing files (creates correct rows) and reconcile deletes the stale orphans; a re-sample went 40/40 `206` (from 24/16).

**Detect every pass (cheap, black-box):** sample ~40 random non-audiobook tracks and count `/Audio/{id}/stream` Range responses — any material 404 rate is this class (or a genuinely missing file). Confirm from the backend: `kubectl logs deploy/yay-tsa-v2-production-backend --since=15m | grep -cE 'idx_images_one_primary|Failed to process file'` must be 0 after a scan (`Library scan complete: N tracks processed`). **General rule: never delete-then-insert a row sharing a unique index within one Hibernate tx without a flush between — inserts-before-deletes ordering violates the constraint. This is the same family as the documented `resume_position` upsert race and the outbox varchar drift: an ORM write pattern that passes in isolation but corrupts under a real constraint.**

## Affinity §302 invariant is STALE post play_history fold — reconcile against signals + non-adaptive history, not PLAY_START alone

The invariant documented under "Affinity worker: signal-time cursor migration…" (`SUM(play_count) == PLAY_START signal count`, `SUM(skip_count) == SKIP_* count`) predates the play_history fold (`b95be7be`) and no longer holds — `AffinityAggregator` now folds BOTH `playback_signals` AND non-adaptive `play_history` (`source IS DISTINCT FROM 'adaptive'`) on separate watermarks (log line: "N pairs from signals … M pairs from play_history"). Current healthy reconciliation:

- `SUM(play_count)` = (non-audiobook PLAY_START signals) + (non-audiobook non-adaptive play_history plays). It is **less than** total play events because audiobooks are excluded from the fold — **`SUM(play_count) < PLAY_START + non-adaptive-history` is the HEALTHY signal; the double-count trap inflates it ABOVE total events.** (Observed 10494 vs 6581 PLAY_START + 4520 history = 11101 total − ~607 audiobook plays.)
- `SUM(skip_count)` = SKIP*\* signals + play_history rows with `skipped=true` (both non-audiobook). Observed 1733 = 371 signal-skips + ~1362 history-skips — do NOT expect it to equal SKIP*\* signals alone.
  Reconcile against the two-source formula (or just assert no runaway inflation above total events); the old signals-only equality will always read as a false "mismatch".

## FavoriteOrder ItemIds needs uuid ITEM format, not array-field format (schemathesis positive_data_acceptance)

`OpenApiConformanceConfig.UUID_BODY_FIELDS` applies `format=uuid` to the field's own schema — correct for scalar id fields, but a no-op for an **array** field (arrays carry no `format`; the ITEMS do). `FavoriteOrderRequest.ItemIds` was only in `NON_BLANK_ARRAY_ITEM_FIELDS` (`items.minLength=1`), which still lets schemathesis generate a single whitespace char (blank) → the `isNotBlank()` handler 400s → `positive_data_acceptance` failure. Fix (`dfadee26`): new `UUID_ARRAY_ITEM_FIELDS` map applying `items.format="uuid"`; `reorderFavorites` ignores unknown-but-valid uuids so a valid-uuid array is a clean 200. **Pattern: for id-ARRAY body fields, constrain `items.format`, not the field's `format`.** The irreducible residue (create endpoints that 400 on valid-format-but-nonexistent ids like `POST /Playlists` "Unknown tracks", empty `command`) can't be schema-fixed — filed `nikolay-e/autoqa#30` to reconcile clean RFC7807 4xx on `positive_data_acceptance` as informational (like the existing text/html-4xx reconciliation).

## Reading the INTERNAL autoqa run (Argo Workflows, no `gh run` — 2026-07-12)

Post-deploy QA is an in-cluster Argo Workflow (`yay-tsa-autoqa-*` in namespace `argo-workflows`), NOT a GitHub Action — there is no autoqa pin in `.github/workflows/` to bump and no `gh run` for it. To read a run: `kubectl get workflows -n argo-workflows` (the newest `yay-tsa-autoqa-*` whose `spec.arguments.parameters[commit-sha]` == the deployed SHA is authoritative), then `kubectl logs -n argo-workflows <workflow-pod> -c main` (failed workflow pods survive — `podGC: OnWorkflowSuccess`). The workflow image ref (`ghcr.io/nikolay-e/autoqa:main-<sha>`) IS the autoqa version — and it is a **hand-pinned full SHA** in `gitops:kubernetes/ci/ci-platform/workflow-templates/autoqa.yaml` (ci-platform has NO Image Updater annotations; the earlier "tracking via Image Updater, no manual bump" claim was wrong). After pushing to `nikolay-e/autoqa`, bump the pin in gitops (verify the tag exists first: `gh api /users/nikolay-e/packages/container/autoqa/versions`) — workflows pick the template at submit time, so in-flight runs keep the old image. `.status.nodes` is a MAP not an array, so array-form jsonpath errors; read the archived `main.log` from the pod instead. The gate prints `## AutoQA — final gate` with a per-tool table + `### Findings`; `exit status 1` with `schemathesis [fail] (BLOCKING)` is the usual red. **Triage: read every `FAILURES` block and classify each — a `[502]`/`[520]` body whose title is `yay-tsa.com | 502: Bad gateway` / `520: Web server…` is a Cloudflare edge page from the single-replica rollout window (§508), NOT a backend bug; re-verify on the settled backend (`POST /Sessions/Playing` → 204, `DELETE /v1/groups` → 405) to confirm it's transient. What's left after removing the CF-transients is the real signal.**

## Raw DB-exception message leaked the SQL/schema into the RFC7807 `detail` (2026-07-12, fixed)

`SpringTransactionalCommandExecutor` mapped `catch (DataIntegrityViolationException) -> Failure.InvariantViolation(e.message)` — and `e.message` is the full Hibernate/JDBC batch-abort string (`could not execute batch … insert into core_v2_playlists.playlists (created_at,description,…) values (…) was aborted: ERROR: invalid byte sequence …`). That message becomes the client-facing RFC7807 `detail` on ANY write path that trips a DB constraint, disclosing schema/table/column names + bound values. Surfaced by the schemathesis `POST /Playlists` fuzz sending a NUL (0x00) in `Name` — a clean 400, but the `detail` leaked the INSERT. Fix (defense-in-depth): log the cause server-side (`log.warn`), return a generic `Failure.InvariantViolation("The request violates a data constraint")`. Only DB-level violations are genericized; domain `InvariantViolation`s (e.g. "Unknown tracks") keep their intended message. **Detect every pass: `curl` any write endpoint with a constraint-tripping body and grep the `detail` for `insert into`/`core_v2_`.**

**Correction (a later pass, same day):** the pass-1 claim that "this playlist-name NUL produces 0 ERROR-log spam" was WRONG — it was measured on a fresh pod BEFORE any NUL request hit it. A NUL name DOES trip Hibernate's `SqlExceptionHelper` (`SQLState 22021`, `invalid byte sequence for encoding "UTF8": 0x00`) at ERROR with the raw INSERT, exactly §231's class, before the executor's catch runs. The genericization only fixed the CLIENT `detail`; the SERVER log still spammed. Complete fix: a global Jackson NUL-stripping String deserializer in `JacksonConfig` (delegates to the real String deserializer, `filter { it.code != 0 }` — construct the NUL with `0.toChar()`, never a source escape). It scrubs NUL from EVERY deserialized string at the edge, so no write field (playlist/group name, preferences, …) ever reaches the DB with a NUL; the create now succeeds with the NUL stripped (200) instead of 400. Regression: `PlaylistsIntegrationTest."a NUL byte in a name is stripped at the edge, never sent to the DB"` (NUL name → 200, GET `/Items/{id}` Name == sanitized). **Detect: after any fuzz, `kubectl logs deploy/<backend> | grep -c 22021` must be 0.** General lesson: a "0 log spam" measurement on a just-restarted pod proves nothing — the triggering request may not have arrived yet; re-measure after the fuzz window, or reason from the code path.

## The Edit-tool NUL trap bites COMMENTS too, and git flags the file `Binary` (2026-07-12)

§235 documented that writing a NUL source escape via the Edit tool plants a real 0x00 byte. It also fires when the escape sits in a **comment** (a sentence _describing_ NUL handling) — the compiler/tests pass (comments are stripped from bytecode, so `:app:test` was green), but `git diff` shows the file as `Binary files … differ` and any later reader/tool chokes. **Before committing a file that mentions NUL / a 0x00 escape, check `git diff` for a `Binary files differ` line** (or `LC_ALL=C tr -cd '\000' < file | wc -c`); strip with a Python `b.replace(b"\x00", b"")` round-trip. Construct NUL in test code with `0.toChar()` and describe it in prose without an actual escape sequence.

## Track title leaked a filename audio extension (M4 mechanical class, 2026-07-12)

One track rendered as `Lament.flac` in the library UI (the source's ID3 TITLE tag was literally the filename). `String.usableTag()` accepts it (not the `^[#?\s]+$` placeholder form) and the scanner's `nameWithoutExtension` only strips the FILENAME's extension, not a tag that already carries one. Fix: `LibraryWriter.stripTrailingAudioExtension` drops a trailing `\.(flac|mp3|wav|ogg|m4a|aac|wma|opus)$` from the derived title (a real title never ends in an audio extension). Prevention is scan-time; the existing row won't re-probe (mtime unchanged), so a one-shot `UPDATE core_v2_library.entities SET name=regexp_replace(name,'\.(flac|mp3|…)$','','i'), sort_name=lower(…) WHERE name ~* '\.(…)$'` fixes the live row. Detect: `SELECT count(*) … WHERE entity_type='TRACK' AND name ~* '\.(flac|mp3|wav|ogg|m4a|aac|wma|opus)$'` should be 0.

## ReflectExternalPlayback OCC conflict was WARN-spamming (2026-07-12, fixed)

`reflectDevicePlayback` retries the reflection twice (`repeat(2)`) to ride out an OCC race, but logged `log.warn("ReflectExternalPlayback failed …")` on EVERY failed attempt — including the first, which is then retried and usually succeeds. Under normal concurrent progress reporting this WARNed ~15×/4h across active devices for a benign, self-correcting lost race (the next `/Sessions/Playing/Progress` re-reflects fresh state).

**Pass-1 fix was INCOMPLETE — only downgraded `Conflict`/`StorageConflict` to debug, kept WARN for other failures.** The next autoqa run then showed **366 WARN** in one window: the schemathesis fuzzer POSTs `/Sessions/Playing` with a non-existent `ItemId` (the canonical fuzz UUID `e3e70682-…`), so reflection fails with `InvariantViolation(rule=Unknown tracks: …)` — a NON-conflict failure my incomplete fix still WARNed on. The reflection is explicitly best-effort ("a failure must never break the 204 report contract"), and a `CommandResult.Failed` is always a typed business outcome (unknown/deleted/external track, lost lease, version race) — NONE operator-actionable. Complete fix: log EVERY typed reflection failure at `debug`; retry only `Conflict`/`StorageConflict` (re-read fresh version). **Lesson: when downgrading a best-effort background op's failure logging, cover ALL its typed `Failure` variants, not just the one you observed in prod — a fuzzer surfaces the others. Verify with `kubectl logs deploy/<backend> | grep -c 'ReflectExternalPlayback'` (a WARN-level match) == 0 after an autoqa run.** Same doctrine as §294 (retry-in-progress = debug, not warn).

## PWA "Update available" prompt lagged ~1h behind a deploy (2026-07-12, fixed)

The in-app update flow is correct-by-design and app-driven: `registerSW` (vite-plugin-pwa) `onNeedRefresh` → `UpdatePrompt` toast ("Update available / Restart") → `updateServiceWorker(true)`. **A browser never surfaces a generic "update this PWA" affordance — the app must detect the waiting SW and prompt.** The bug: `main.tsx` only called `registration.update()` on a **1-hour** `setInterval` and nowhere else, so on an open/installed PWA the prompt could surface up to an hour after a new build shipped (reads as "the update button never appears"). Fix: also `registration.update()` on `document.visibilitychange` → visible (immediate check when the user returns to the tab, which is exactly when they'd want it), and shorten the interval to 15 min. Non-destructive (`update()` only checks for a new SW; the waiting SW still activates only on the user's Restart click). Verify: deploy a new build, background then re-focus the tab, the "Update available" toast appears within seconds.

## Pre-commit stage split: a hook that needs built artifacts must live in the SAME stage as the hook that builds them (2026-07-14)

The 2026-07-13 leverage pass moved the heavy hooks (`tsc-core`, `tsc-web`, `knip`, `jscpd`, `type-coverage`) to `stages: [pre-push]` but left `eslint` in the default stage. Locally everything passed (dist exists), but on a cold CI runner the default-stage `pre-commit run --all-files` now ran eslint BEFORE anything built `packages/core/dist` — and `@yay-tsa/core` resolves via package.json `exports` → `./dist/index.js`, so import/no-unresolved exploded with 158 errors on every `@yay-tsa/*` import (plus cascading `no-unsafe-*`). The old green runs worked only because `tsc-core` (earlier in config order, default stage) built dist as a side effect before eslint ran. Fix: `eslint` → `stages: [pre-push]` too (typed lint belongs with the tsc hooks; config order keeps tsc first within the stage). **Reproduce/verify cold-CI locally: `mv packages/core/dist /tmp/x` → eslint fails; `npm run type-check` → eslint clean.** General rule: when splitting hook stages, trace which hooks depend on artifacts other hooks produce — the dependency is invisible while your working tree is warm.

## Schemathesis int64-overflow class recurs on every NEW Long body field (§504 follow-up, 2026-07-14)

`POST /v1/groups/{groupId}/schedule` failed `positive_data_acceptance`: `ScheduleRequest.positionMs`/`expected_epoch` (Kotlin `Long`) had no schema `maximum`, so a 7.5e30 fuzz value was "schema-compliant" but overflowed Jackson's Long → 400 "Invalid request body". Fixed by adding `ScheduleRequest` to `NON_NEGATIVE_INT_BODY_FIELDS` in `OpenApiConformanceConfig`. When ADDING any endpoint with numeric body fields, add its DTO there in the same commit — the customizer only covers mapped schema names, and every unmapped Long field is a future autoqa red.

## /Lyrics/\* are external-proxy endpoints — excluded from schemathesis (2026-07-14)

`GET /Lyrics/{trackId}` and `POST /Lyrics/{trackId}/fetch` proxy each request to lrclib.net (bounded 5s/8s timeouts, but schemathesis still recorded a tool-side read timeout as a Network Error, and fuzzing hammers a free external API). Added both to `schemathesis-exclude-paths` in gitops `sensor-yay-tsa-image.yaml` — same lever as the OAuth/Logout exclusions. Lyrics stay covered by the crawler's real login flow + integration tests.

## Karaoke on-demand priority (closes #272, 2026-07-14)

`POST /Karaoke/{trackId}/process` now also calls `KaraokeOnDemandPort.requestImmediate` (new port in `core-application/karaoke/port`, implemented by `KaraokeProcessor`): the id goes into a `ConcurrentLinkedQueue` drained BETWEEN batch tracks (so a user request waits one separation, not the whole backlog), and an idle worker picks it up immediately via the same `batchInFlight` CAS + dedicated `karaoke-separation` executor. `processTrack` is idempotent (skips ready/exhausted), so a track being both user-requested and batch-selected never double-separates. The port is injected NULLABLE into the controller — the processor bean exists only when `yaytsa.karaoke.enabled=true`. Verify live: toggle karaoke on an unprocessed track → PROCESSING → READY in ~minutes even with a batch running.

## Dependabot majors are deferred with comments, not merged and not closed (2026-07-14)

The re-armed dependabot opened 9 PRs at once. Merged when green: minor/patch groups + SHA-pinned action bumps. Deferred WITH a comment naming the specific blocker (never silently): Spring Boot 4/Kotlin 2.4/Kotest 6 gradle group (the dedicated v2 migration, tracker = the PR itself), TypeScript 7 + eslint-plugin-boundaries 7 inside the npm group (config uses v6 selector syntax), cuda 13.3 (needs R580+ host driver validation on the single GPU node), python 3.14 (essentia-tensorflow has no cp314 wheels), temurin 25 (Boot 3.4 targets ≤23), node 26 (non-LTS until October). A grouped PR that mixes majors with patches is deferred as a unit — don't cherry-pick inside dependabot branches.

## :app:test flake lottery — suspected FOURTH class: query-count guard under full-suite state (2026-07-14, tracking #284)

`ItemsPaginationIntegrationTest` "no per-album artist N+1" failed once on CI (small=5, large=12 — systematic across all 5 MIN-samples, so NOT the additive background-scheduler noise its MIN-sampling defends against), on a commit that touched no /Items path; the same commit's rerun + isolated and full-suite local runs (with `--no-build-cache`!) are green. Tracked in #284 with the diagnosis playbook (SQL-log the class when it re-fires, diff isolated-vs-suite statements). **Gradle gotcha that cost a round: `:app:cleanTest :app:test` can still be served FROM-CACHE by the local build cache — a "full local rerun" proves nothing unless `--no-build-cache` (or `--rerun-tasks`) is passed; check the task line says `executed`, not `FROM-CACHE`/`UP-TO-DATE`.**

## Album/artist favorite hearts were dead controls — favorites are per-track BY DESIGN (2026-07-14)

Browser QA caught `POST /UserFavoriteItems/<albumId>` → 404 from the AlbumDetailPage heart: the preferences domain stores TRACK favorites only, album/artist "favorites" are DERIVED server-side (an album is favorite when it holds ≥1 favorited track — see `starredAlbums` and the Items favorites branch), and the Subsonic adapter explicitly `rejectAlbumOrArtistFavorites`. The PWA's 2026-06-05 "unify hearts" pass added album/artist FavoriteButtons anyway — permanently empty (album `toBaseItem` emits no `UserData`) and 404 on click. Fix: removed both page-level hearts; track hearts + derived tabs are the whole model. **Trap that hid it: the favorites e2e ran against MOCKED /api routes, so the optimistic patch + mock 200 kept it green while prod 404'd** — a mocked e2e proves UI mechanics, never endpoint support; the spec now toggles a real track heart (`track-favorite-button`). If a future feature wants first-class album/artist favorites, that's a domain change (preferences aggregate), not a UI patch.

## Feature-extractor starvation: parked failures + the essentia WARN flood (2026-07-14)

`processed=6 failed=19` nightly with `batchLimit: 25` = the 19 permanently-failing files (WMA rips, a hidden-data WAV, >30min full-album FLACs) sit at the head of `ORDER BY created_at` and eat the whole batch — the 2163-track embedding backlog effectively never drains (same OFFSET-0 starvation class as the MetadataEnricher §304). Fix: `core_v2_ml.extraction_failures` (V007) + `EXTRACT_MAX_ATTEMPTS=3` anti-join in `_pending_tracks`; success deletes the row. Diagnosis was blind because essentia's TF wrapper spams `[ WARNING ] No network created...` 30k+ lines/run, rotating the per-track failure lines out of `kubectl logs` — now silenced via `essentia.log.warningActive = False`. Check every pass: `SELECT count(*) FROM core_v2_ml.track_features` should GROW ~`batchLimit`/night until the backlog drains; `SELECT fail_count, last_error, count(*) FROM core_v2_ml.extraction_failures GROUP BY 1,2` explains what got parked and why. A radio seed picked from a NEW track often returns `degraded: no_embedding` until the backlog drains — that's the backlog, not the similarity path (verify with seeds from `track_features WHERE embedding_mert IS NOT NULL`).

## Internal autoqa gate now reconciles CF-edge 5xx and clean RFC7807 4xx (autoqa e133303)

The two permanent-red classes are gate-reconciled upstream (closes autoqa#29/#30): schemathesis FAILURES blocks whose statuses are all CF-edge (502/504/52x) with a Cloudflare page body (both shapes: `<center>cloudflare</center>` AND the branded `<host> | 502: Bad gateway` title — the report truncates the branded page before the word "cloudflare") → informational; positive_data_acceptance blocks answered by a structured `{"type":...}` problem+json 4xx → informational. Origin 5xx and non-problem bodies still block. Reconciliation total now parses the summary's `X found Y unique failures` (X = failing operations = FAILURES blocks); the per-phase `N failed` max both over- and under-counts merged blocks. App-side, the positive-acceptance offenders got real domain declarations instead of exclusions: maxLength from the DB varchar bounds (group name 200, playlist Name 500, toDeviceId 255, signal_type 30), `pattern: \S` on all NON_BLANK fields, an enum for `CommandRequest.command`, uuid items for `CreatePlaylistRequest.Ids`, and the `Map<String, Any?>` signals body replaced with a typed `SignalRequest` (untyped map bodies = unconstrained schema = fuzz junk that only fails at the DB — same rule as `Map<String, Any>` responses, §149).
