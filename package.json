{
  "name": "yaytsa",
  "version": "0.3.11",
  "private": true,
  "description": "A portable music client for Jellyfin",
  "keywords": [
    "jellyfin",
    "yaytsa",
    "music",
    "client"
  ],
  "license": "MIT",
  "author": "",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:core && npm run build:platform && cd packages/web && npm run build",
    "build:core": "cd packages/core && npm run build",
    "build:platform": "cd packages/platform && npm run build",
    "check:bundle": "bundlewatch",
    "check:circular": "madge --circular --extensions ts packages/",
    "check:duplication": "jscpd .",
    "check:types": "type-coverage --at-least 90 --ignore-catch",
    "check:unused": "knip --no-progress",
    "dev": "npm run build:core && npm run build:platform && cd packages/web && npm run dev",
    "format": "prettier --write .",
    "generate:splash": "node scripts/generate-splash-screens.js",
    "lint": "prettier --check .",
    "pre-commit": "pre-commit run --all-files",
    "prepare": "pre-commit install && pre-commit install --hook-type commit-msg",
    "sync:version": "node scripts/sync-version.js",
    "type-check": "cd packages/core && npx tsc --noEmit",
    "postversion": "npm run sync:version && git add -A"
  },
  "dependencies": {
    "svelte-gestures": "^5.2.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "@eslint/js": "^9.18.0",
    "@typescript-eslint/eslint-plugin": "^8.48.1",
    "@typescript-eslint/parser": "^8.19.1",
    "bundlewatch": "^0.4.1",
    "eslint": "^9.18.0",
    "eslint-plugin-svelte": "^3.13.1",
    "globals": "^16.5.0",
    "jscpd": "^4.0.5",
    "jsdom": "^27.1.0",
    "knip": "^5.71.0",
    "madge": "^8.0.0",
    "prettier": "^3.7.4",
    "sharp": "^0.34.4",
    "stylelint": "^16.26.1",
    "stylelint-config-recommended": "^17.0.0",
    "stylelint-config-standard": "^39.0.1",
    "svelte-eslint-parser": "^1.4.1",
    "type-coverage": "^2.29.7",
    "vitest": "^4.0.15",
    "yaml": "2.8.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
