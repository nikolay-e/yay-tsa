name: Code Review

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write
  id-token: write

jobs:
  review:
    if: github.event.pull_request.user.login != 'dependabot[bot]'
    uses: nikolay-e/treemapper-claude-code-review-action/.github/workflows/claude-code-review.yml@1deb98c84c5519a738e905e1a47679bfc832554d # main
    with:
      model: claude-opus-4-6
      max_turns: 15
    secrets:
      anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
      treemapper_issues_token: ${{ secrets.TREEMAPPER_ISSUES_TOKEN }}
